# Functional requirements

| ID | Requirement | URS | Design | Tests |
|---|---|---|---|---|
| FRS-BB-001 | Patient identity match at issue requires two tokens that match stored MRN/DOB/name fields (`PatientIdentityMatchRule`). | URS-BB-001 | `IssueGate` `ISS-IDENTITY` | Domain + issue integration |
| FRS-BB-002 | Antibody history is append-only. Add requires `immuno.record`; deactivation requires a reason and `immuno.override` in the Application service. Deactivated rows remain visible and continue to block electronic XM. An open workup HardStops chart add and deactivate (`FRS-BB-171`). | URS-BB-002 | `AntibodyHistory`; `ImmunoAuthorizationRule`; `ElectronicCrossmatchEligibilityService` | `ElectronicXmHistoryRegressionTests`; Phase 3 antibody permission tests |
| FRS-BB-003 | Merged patient records HardStop accession, visits, orders, result entry/verify/correct/invalidate, immunohematology writes, special requirements, issue, allocation, compatibility evaluation, and crossmatch (`PAT-MERGED-INACTIVE`). Inactive status is not blocked. Staff with `patient.merge` can merge a duplicate into the survivor (`POST /api/patients/{id}/merge` and ADT A18/A40). Demographic `patient.write` does not grant merge. History is reassigned, not deleted. Discordant ABO/Rh HardStops the merge. | URS-BB-015, URS-BB-017 | `PatientMergeService`; `PatientMergeRule`; patient detail merge UI | `PatientMergeServiceTests`; Phase 8 seed tests; Phase 5 HL7 merge tests; `MergedPatientClinicalUseTests` |
| FRS-BB-010 | Specimen expiration is computed from collection time and alloimmunization-risk policy. Expired or non-accepted specimens HardStop issue, result entry, and result verification. | URS-BB-003, URS-BB-016 | `SpecimenExpirationRule`; `ResultService.ValidateSpecimenForEntryAsync` | Domain expiry boundary; `MergedPatientClinicalUseTests` |
| FRS-BB-020 | Verified results are not overwritten. Corrections create a new version and retain the original. | URS-BB-004 | `ResultService` | Result versioning tests |
| FRS-BB-079 | Result provenance is stored as Manual, Instrument, Interface, or Calculated. Instrument and interface entries start `PendingVerification`. Manual and calculated entries start `Entered` and may be submitted for verification. | URS-BB-047 | `ResultSource`; `ResultLifecycleRule`; `ResultService` | `ResultLifecycleRuleTests`; `ResultLifecycleTests` |
| FRS-BB-089 | ABO/Rh panel entry and catalog-interpretation saves set `ResultSource.Calculated` unless the observation already arrived as Instrument or Interface. Test-definition create, update, and clone write `AuditEventType.TestChange` (activate/deactivate remain Activate/Deactivate). | URS-BB-047, URS-BB-056, URS-BB-011 | `ResultProvenanceRule`; `ResultService`; `TestDefinitionAdminService` | `ResultProvenanceRuleTests`; `ResultLifecycleTests`; `AdminConfigTests` |
| FRS-BB-082 | Inbound ORU posts through `ResultService.EnterFromInterfaceAsync`. Source is Interface; status is `PendingVerification`. Verified rows are not overwritten. The order must belong to the PID patient and have a linked accepted specimen. OBX-11 empty/F/C/P/R may post; other statuses NAK (OCD-019). | URS-BB-049 | `Hl7OruMapper`; `InterfaceResultAcceptanceRule`; `Hl7InboundProcessor` | `InterfaceResultAcceptanceRuleTests`; `Hl7MapperTests`; `Phase5Hl7Tests` inbound ORU |
| FRS-BB-077 | Invalidating a result requires `result.invalidate` and a reason. A verified result is superseded by a new Invalidated version; the original row stays Verified. An unverified correction is marked Invalidated and the prior verified row becomes current again (OCD-016). Posted ABO/antibody history is not auto-reverted (OCD-017). | URS-BB-046 | `ResultService.InvalidateResultAsync`; `ResultAuthorizationRule` | `ResultLifecycleTests`; `ResultAuthorizationRuleTests` |
| FRS-BB-021 | Manually recording current ABO/Rh requires `immuno.override` in the Application service (not only the API). Antibody add and antigen-profile writes require `immuno.record`. Antibody deactivate requires `immuno.override`. A reason is required for manual ABO and deactivate. History is append-only. | URS-BB-019, URS-BB-002 | `ImmunohematologyService`; `ImmunoAuthorizationRule` | `ImmunoAuthorizationRuleTests`; Phase 3 immuno permission tests |
| FRS-BB-022 | Patient ABO/Rh verification applies `SelfVerifyRule` when `Result.BlockAboSelfVerify` is on (default). `MarkComplete` does not auto-verify ABO/Rh. Current `PatientBloodTypeHistory` is written only after a successful verify. | URS-BB-020 | `ResultService`; `SelfVerifyRule` | Phase 3 ABO self-verify and MarkComplete tests |
| FRS-BB-023 | Adding a special transfusion requirement requires `immuno.record` in the Application service. Deactivating one requires a reason and `immuno.override`. Callers that skip the API filter cannot change `ISS-SPECIAL-REQ` facts. | URS-BB-021 | `SpecialRequirementService`; `ImmunoAuthorizationRule` | `ImmunoAuthorizationRuleTests`; Phase 3 special-requirement permission test |
| FRS-BB-024 | DIN lookback recall requires `lookback.manage` in the Application service. Each component recall is applied through `InventoryService.RecallForLookbackAsync`; a failed status transition fails the lookback. Selected/Assigned/Allocated/Crossmatched/ReturnPending may move to Recalled. Transfused units are notified, not recalled. Named `Lookback` recall/attempt audits are `FRS-BB-174`. | URS-BB-022 | `LookbackService`; `LookbackAuthorizationRule`; `InventoryStatusTransition` | `LookbackAuthorizationRuleTests`; `InventoryStatusTransitionTests`; Compliance lookback tests |
| FRS-BB-030 | Compatibility decisions return structured `RuleResult` codes and messages. The engine never downgrades a HardStop. | URS-BB-005 | `RuleEvaluation` | Aggregation tests |
| FRS-BB-031 | `IssueGate` evaluates identity, specimen, ABO/Rh, allocation, XM, special requirements, appearance, location, emergency ABO, antigen-negative, and autologous/directed rules before issue. | URS-BB-006 | `IssueGate` | `IssueGateSafetyRegressionTests` |
| FRS-BB-032 | Autologous/directed issue to a non-designated patient is HardStop `ISS-AUTO-DIR` inside the issue gate. | URS-BB-008 | `AutologousDirectedRule` | Domain + issue integration |
| FRS-BB-033 | Electronic XM eligibility is an evaluated service (`eligible` + criteria), not a checkbox. Any antibody-history row fails `XM-EC-HISTORY`. An open antibody-identification workup fails `XM-EC-ABID-OPEN`; serologic XM remains available and warns (`ABID-XM-OPEN`) without blocking. Allocate and issue open-workup warnings are `FRS-BB-193`. | URS-BB-009 | `ElectronicCrossmatchEligibilityService`; `ElectronicCrossmatchEligibilityRule`; `AntibodyIdentificationHistoryPostRule` | Eligibility tests; `CrossmatchRuleTests`; `Phase4IssuingTests.RecordCrossmatch_OpenAntibodyIdWorkup_WarnsAndRecords` |
| FRS-BB-040 | Emergency/MTP issue is a distinct type. Missing XM becomes an overridable Warning; ABO/identity/status HardStops still apply unless a listed emergency warning conversion exists. Override reason and authorizer are required. Emergency/MTP also requires `issue.emergency-release`. Non-emergency warning overrides require `issue.override`. | URS-BB-006 | `IssuingService`; `IssueAuthorizationRule` | Phase 4 emergency tests; `IssueAuthorizationRuleTests`; Phase 8 seed tests |
| FRS-BB-041 | At most one `Reserved` allocation and one `Issued` issue exist per unit (filtered unique indexes). Concurrent conflicts fail closed. | URS-BB-007 | `IX_Allocations_OneReservedPerUnit`, `IX_Issues_OneOpenIssuePerUnit` | `AllocationIssueConcurrencyTests` |
| FRS-BB-050 | Inventory transitions are allow-listed. Each transition writes `InventoryStatusHistory` + audit. | URS-BB-010 | `InventoryStatusTransition` | Transition tests |
| FRS-BB-051 | Unit ABO/Rh retype is entered, then verified. Status stays Received until verify. Matching verify → Available; mismatch → Quarantine. Default blocks the entering user from verifying (`RES-SELF-VERIFY`, `Inventory.BlockRetypeSelfVerify`). | URS-BB-018 | `ProductRetypeService`; `SelfVerifyRule` | `ProductRetypeServiceTests` |
| FRS-BB-052 | Releasing a unit from quarantine requires `inventory.release` in the Application service (not only the API). Dual-verifier policy still applies. Named release `ProductStatus` audits are `FRS-BB-178`. | URS-BB-023 | `InventoryService`; `InventoryAuthorizationRule` | `InventoryAuthorizationRuleTests`; InventoryService permission test |
| FRS-BB-053 | Converting a directed unit to allogeneic inventory requires `inventory.release` in the Application service (not only the API). Reason and dual-verifier policy still apply. | URS-BB-024 | `InventoryService`; `InventoryAuthorizationRule` | `InventoryAuthorizationRuleTests`; InventoryService directed-conversion permission test |
| FRS-BB-054 | Releasing a unit from operational hold requires `inventory.release` in the Application service (not only the API). Named release `ProductStatus` audits are `FRS-BB-178`. | URS-BB-025 | `InventoryService`; `InventoryAuthorizationRule` | `InventoryAuthorizationRuleTests`; InventoryService hold-release permission test |
| FRS-BB-055 | Verifying a test result or unit ABO/Rh retype requires `result.verify` in the Application service (not only the API). Self-verify policy still applies. | URS-BB-026 | `ResultService`; `ProductRetypeService`; `ResultAuthorizationRule` | `ResultAuthorizationRuleTests`; Phase 3 and ProductRetype permission tests |
| FRS-BB-056 | Allocating a unit requires `compatibility.allocate` in the Application service. Recording a crossmatch requires `compatibility.crossmatch`. Callers that skip the API filter cannot reserve a unit or record XM used at issue. | URS-BB-027 | `CompatibilityService`; `CompatibilityAuthorizationRule` | `CompatibilityAuthorizationRuleTests`; PatientAllocation permission tests |
| FRS-BB-057 | Issuing a unit requires `issue.create` in the Application service (not only the API). Emergency/MTP still also requires `issue.emergency-release`. | URS-BB-028 | `IssuingService`; `IssueAuthorizationRule` | `IssueAuthorizationRuleTests`; Phase 4 issue-create permission test |
| FRS-BB-058 | Entering a result or unit retype requires `result.enter` in the Application service. Correcting a verified result requires `result.correct` (including save-as-correction). Invalidating a result requires `result.invalidate`. | URS-BB-029, URS-BB-046 | `ResultService`; `ProductRetypeService`; `ResultAuthorizationRule` | `ResultAuthorizationRuleTests`; Phase 3 and ProductRetype permission tests; `ResultLifecycleTests` |
| FRS-BB-059 | Accessioning a specimen requires `specimen.accession` in the Application service. Editing collection metadata requires `specimen.edit`. Rejecting a specimen requires `specimen.reject`. | URS-BB-030 | `SpecimenService`; `SpecimenAuthorizationRule` | `SpecimenAuthorizationRuleTests`; Phase 3 specimen permission tests |
| FRS-BB-061 | Updating patient demographics requires `patient.write` in the Application service (not only the API). ADT inbound still writes through the HL7 processor, not `PatientService`. | URS-BB-031 | `PatientService`; `PatientAuthorizationRule` | `PatientAuthorizationRuleTests`; PatientService permission test |
| FRS-BB-062 | Dividing, pooling, or applying a product modification requires `inventory.modify` in the Application service (not only the API). Result units still land in Quarantine. | URS-BB-032 | `BloodProductModificationService`; `InventoryAuthorizationRule` | `InventoryAuthorizationRuleTests`; BloodProductModificationService permission test |
| FRS-BB-063 | Correcting unit ISBT identity requires `inventory.correct-identity` in the Application service (not only the API). Post-issue corrections remain locked unless the post-event path is used. Named Correct payloads with correction row id are `FRS-BB-187`. | URS-BB-033 | `ComponentIdentityCorrectionService`; `InventoryAuthorizationRule` | `InventoryAuthorizationRuleTests`; ComponentIdentityCorrectionService permission test |
| FRS-BB-064 | Releasing an allocation requires `compatibility.allocate` in the Application service (not only the API). Callers that skip the API filter cannot return a reserved unit to Available. | URS-BB-034 | `CompatibilityService`; `CompatibilityAuthorizationRule` | `CompatibilityAuthorizationRuleTests`; PatientAllocation release permission test |
| FRS-BB-065 | Completing an ISBT scan session or creating a manual component requires `inventory.receive` in the Application service (not only the API). Walk-in `InventoryService` receive remains a separate path. Named receive `ProductStatus` audits are `FRS-BB-175`. | URS-BB-035 | `ScanSessionService`; `ManualComponentEntryService`; `InventoryAuthorizationRule` | `InventoryAuthorizationRuleTests`; ISBT manual-entry permission test |
| FRS-BB-066 | Updating a reaction investigation, recording CBER notification, or recording the written fatality report requires `reaction.investigate` in the Application service. Opening from a suspected transfusion remains an automatic issue-path write. Named `ReactionInvestigation` audits are `FRS-BB-173`. | URS-BB-036 | `ReactionInvestigationService`; `ReactionAuthorizationRule` | `ReactionAuthorizationRuleTests`; ReactionInvestigationService permission test |
| FRS-BB-067 | Walk-in receive, expected-arrival confirmation, and normalized-component intake require `inventory.receive` in `InventoryService` (not only the API). ISBT scan-session complete and manual entry remain separately gated. Named receive `ProductStatus` audits are `FRS-BB-175`. Packing-list expect/arrival/cancel named audits are `FRS-BB-176`. | URS-BB-037 | `InventoryService`; `InventoryAuthorizationRule` | `InventoryAuthorizationRuleTests`; InventoryService receive permission test |
| FRS-BB-068 | Returning an issued unit to Available, Quarantine, or Discarded requires `issue.return` in the Application service (not only the API). | URS-BB-038 | `IssuingService`; `IssueAuthorizationRule` | `IssueAuthorizationRuleTests`; Issuing return permission test |
| FRS-BB-069 | Documenting a transfusion requires `transfusion.document` in the Application service (not only the API). Callers that skip the API filter cannot mark a unit Transfused. | URS-BB-039 | `IssuingService`; `IssueAuthorizationRule` | `IssueAuthorizationRuleTests`; Issuing transfusion-document permission test |
| FRS-BB-071 | Recording ward receipt of an issued unit requires `transfusion.document` in the Application service (not only the API). Callers that skip the API filter cannot unblock transfusion documentation. | URS-BB-040 | `IssuingService`; `IssueAuthorizationRule` | `IssueAuthorizationRuleTests`; Issuing ward-receipt permission test |
| FRS-BB-072 | Discarding a unit requires `inventory.discard` in the Application service (not only the API). Dual-verifier checks still apply. | URS-BB-041 | `InventoryService`; `InventoryAuthorizationRule` | `InventoryAuthorizationRuleTests`; InventoryService discard permission test |
| FRS-BB-073 | Transferring a unit between locations requires `inventory.transfer` in the Application service (not only the API). Named transfer `ProductStatus` audits are `FRS-BB-184`. | URS-BB-042 | `InventoryService`; `InventoryAuthorizationRule` | `InventoryAuthorizationRuleTests`; InventoryService transfer permission test |
| FRS-BB-074 | Direct inventory recall requires `inventory.recall` in the Application service. Lookback DIN recall remains `lookback.manage` only (OCD-014) via `RecallForLookbackAsync`. Named direct-recall `ProductStatus` audits are `FRS-BB-179`. | URS-BB-043 | `InventoryService`; `LookbackService`; `InventoryAuthorizationRule` | `InventoryAuthorizationRuleTests`; InventoryService recall permission tests |
| FRS-BB-075 | Creating or updating a quality-system deviation requires `deviation.manage` in the Application service (not only the API). Named `Deviation` audits are `FRS-BB-172`. | URS-BB-044 | `DeviationService`; `DeviationAuthorizationRule` | `DeviationAuthorizationRuleTests`; DeviationService permission tests |
| FRS-BB-076 | Creating a patient record requires `patient.write` in `PatientService` (not only the API). ADT inbound still writes through the HL7 processor, not `PatientService`. | URS-BB-045 | `PatientService`; `PatientAuthorizationRule` | `PatientAuthorizationRuleTests`; PatientService create permission test |
| FRS-BB-077 | Saving a unit blood attribute requires `inventory.receive` in `InventoryService` (not only the API). | URS-BB-046 | `InventoryService`; `InventoryAuthorizationRule` | `InventoryAuthorizationRuleTests`; InventoryService attribute permission test |
| FRS-BB-081 | Returning a unit to the supplier requires `inventory.receive` in `InventoryService` (not only the API). Named return-to-supplier `ProductStatus` audits are `FRS-BB-183`. | URS-BB-048 | `InventoryService`; `InventoryAuthorizationRule` | `InventoryAuthorizationRuleTests`; InventoryService return-to-supplier permission test |
| FRS-BB-083 | Locating a missing unit or inspecting a damaged unit into quarantine requires `inventory.release` in `InventoryService` (not only the API). Named locate/inspect `ProductStatus` audits are `FRS-BB-182`. | URS-BB-050 | `InventoryService`; `InventoryAuthorizationRule` | `InventoryAuthorizationRuleTests`; InventoryService locate/inspect permission tests |
| FRS-BB-084 | Recording or cancelling an expected inbound packing-list unit requires `inventory.receive` in `InventoryService` (not only the API). Arrival confirmation remains separately gated. Named packing-list `ProductStatus` audits are `FRS-BB-176`. | URS-BB-051 | `InventoryService`; `InventoryAuthorizationRule` | `InventoryAuthorizationRuleTests`; InventoryService expect/cancel permission tests |
| FRS-BB-060 | Audit events are append-only and written in the same save pipeline as the change. Named events capture who/what/when/where (workstation)/old/new/why when applicable. | URS-BB-011 | `BloodBankDbContext`; `IAuditWriter` | Audit tests |
| FRS-BB-078 | Result enter, in-place update, submit-for-verification, verify, correct, and invalidate write named audit events. Opening a patient chart (`GET /api/patients/{id}`) writes `PatientAccess`. List/search does not. Stored `ResultSource` on submit/verify/invalidate/re-entry old/new is `FRS-BB-177`. | URS-BB-011 | `ResultService`; `PatientService.RecordAccessAsync` | `ResultLifecycleTests` |
| FRS-BB-085 | Specimen accession/edit/reject, order create/cancel/link, allocation, crossmatch, emergency/MTP issue, transfusion documentation, role assignment, and role-permission edits write named `AuditEventType` values (`Specimen`, `OrderChange`, `Assignment`, `Crossmatch`, `EmergencyRelease`, `Transfusion`, `UserRole`) instead of generic Create/Update/Issue. | URS-BB-052 | `SpecimenService`; `OrderService`; `CompatibilityService`; `IssuingService`; `UserAdminService` | Phase 3/4/8/10 named-audit assertions |
| FRS-BB-086 | Recording a lookback notification attempt requires `lookback.manage` in the Application service (not only the API). Find/traceback are gated separately (FRS-BB-147). Named `Lookback` attempt audits are `FRS-BB-174`. | URS-BB-053 | `LookbackService`; `LookbackAuthorizationRule` | `LookbackAuthorizationRuleTests`; Compliance lookback attempt permission test |
| FRS-BB-087 | `GET /api/audit-events` filters by entity type/id, named `eventType`, user, and UTC window. The audit page exposes those filters and displays old/new JSON. Unknown event types return 400. There is still no update/delete path. | URS-BB-054, URS-BB-011 | `AuditTrailQuery`; `AuditEndpoints`; `Audit.razor` | `AuditTrailQueryTests`; `AuditPipelineTests` named-event query |
| FRS-BB-088 | Printing a specimen, compatibility, or component label requires `print.label` in `PrintService`. Reprinting a stored job still requires a reason and requires `print.reprint` in the service (not only the API). | URS-BB-055 | `PrintService`; `PrintAuthorizationRule` | `PrintAuthorizationRuleTests`; Phase 6 print permission tests |
| FRS-BB-091 | Updating or cancelling an order, or linking a specimen to an order, requires `patient.write` in `OrderService` (not only the API). Order create remains ungated in the service so inbound ORM can post. Create-time specimen association uses the same core link path without a second privilege check. | URS-BB-057 | `OrderService`; `OrderAuthorizationRule` | `OrderAuthorizationRuleTests`; `OrderServiceAuthorizationTests` |
| FRS-BB-092 | Creating or updating a visit from the workspace requires `patient.write` in `EncounterService` (not only the API). Inbound ADT visit upsert and HL7 order-encounter ensure remain ungated. | URS-BB-058 | `EncounterService`; `EncounterAuthorizationRule` | `EncounterAuthorizationRuleTests`; `EncounterServiceAuthorizationTests` |
| FRS-BB-093 | Manual ABO/Rh history writes `AuditEventType.Override`. Antibody add and deactivate write `AuditEventType.Antibody` (same named type as verified ABID posting). | URS-BB-059, URS-BB-011, URS-BB-019 | `ImmunohematologyService` | Phase 3 `ManualBloodType_RequiresReason_AndWritesAudit`; `Antibody_AddThenDeactivate_RequiresReason` |
| FRS-BB-094 | Manual quarantine, operational hold, mark-missing, and mark-damaged require `inventory.release` in `InventoryService` (not only the API). Locate and inspect remain separately gated. Named place-quarantine/hold `ProductStatus` audits are `FRS-BB-180`. Named mark-missing/damaged `ProductStatus` audits are `FRS-BB-181`. | URS-BB-060 | `InventoryService`; `InventoryAuthorizationRule` | `InventoryAuthorizationRuleTests`; InventoryService quarantine/hold/missing/damaged permission tests |
| FRS-BB-095 | Antigen-profile save/update writes `AuditEventType.Antibody` with old/new (in-place overwrite, OCD-022). Reflex-added order lines from verified results write `AuditEventType.OrderChange` instead of Create. | URS-BB-061, URS-BB-011 | `ImmunohematologyService.SaveAntigenProfileAsync`; `ResultService` reflex | Phase 3 antigen named-audit test; `ReflexRulesTests.VerifyPositiveAbsc_AddsAbidLine_AndShowsOnWorklist` |
| FRS-BB-096 | The expiration sweep requires `inventory.discard` in `InventoryService` (not only the API). Due units still move to Expired; the sweep does not discard. Named expire `ProductStatus` audits are `FRS-BB-185`. | URS-BB-062 | `InventoryService.ExpireDueUnitsAsync`; `InventoryAuthorizationRule` | `InventoryAuthorizationRuleTests`; InventoryService expire permission test |
| FRS-BB-097 | Antibody-identification workups record manufacturer/lot/expiration, cells, antigen profiles, phases, strengths, autocontrol, DAT, selected cells, comments, assist findings, technologist interpretation, and supervisor review. Assist may emit Excluded/Possible/CannotExclude/Historical only — never Identified. Saving reactions, lots, DAT, or specimen scope refreshes stored assistance to the current antigram without identifying or posting. Completion HardStops without interpretation, on assist-Identified findings, and (default) without distinct supervisor acceptance. Changing panel reactions, selected-cell lots, DAT, or specimen scope after interpretation withdraws interpretation and review (`ABID-INTERP-STALE` / `ABID-REVIEW-STALE`) and HardStops complete until both are repeated. Completing with Identified findings while a panel or selected cell lacks an interpretive-phase reaction HardStops (`ABID-INCOMPLETE-RXN`). Completing re-evaluates assistance and warns on leftover `CannotExclude` specificities (`ABID-UNEXCLUDED`) and homozygous selected-cell needs (`ABID-SEL-CELL`) without identifying them. Completing with no Identified findings warns (`ABID-COMPLETE-NONE`) and posts nothing. Clinical completion warnings require a technologist acknowledgment (`ABID-COMPLETE-ACK`) before history posts; acknowledgment does not identify antibodies. Supervisor Accept HardStops Identified sign-off on an incomplete panel or a rejected/cancelled specimen and requires acknowledgment (`ABID-REVIEW-ACK`) for the same clinical warnings. Autocontrol reactive with DAT not performed warns (`ABID-DAT-INDICATED`) at complete and does not HardStop. History is posted only from technologist Identified findings after review. Identified specificities are resolved to the blood-attribute catalog when unique so they drive antigen-negative selection; unmatched Identified specificities warn and post as free-text. Identifying a catalog specificity when the patient is antigen-positive warns (`ABID-INTERP-PHENO` / `ABID-INTERP-GENO`) and does not HardStop. Identifying a specificity that assistance would exclude warns (`ABID-INTERP-EXCLUDED`) at interpret and complete and does not HardStop. Interpretation also warns on leftover `CannotExclude` (`ABID-UNEXCLUDED`), selected-cell need, history remaining, and incomplete reactions without identifying them. The patient workup list and the `/antibody-id` open worklist show specimen accession or Unscoped, with patient MRN on the open worklist. Completing with active antibody history warns (`ABID-HIST-REMAINS`) and does not remove history; a current exclusion of a historical specificity warns (`ABID-HIST-UNDETECTED`). An open in-scope workup HardStops free-text ABID verify that would post history; a completed in-scope workup skips free-text posting and warns on disagreement. Void requires a reason, posts nothing, and is blocked after complete. Voiding while reserved or issued units remain warns (`ABID-VOID-PRODUCTS`) and still voids (`FRS-BB-195`). Expired/inactive lots HardStop new workups and additional lot attach. Opening without a specimen warns (`ABID-WORKUP-UNSCOPED`). An open workup can link a specimen; completed or voided workups HardStop (`ABID-WORKUP-SPECIMEN`). A second overlapping open workup HardStops (`ABID-WORKUP-DUP-OPEN`). Rejected, cancelled, expired, or collected-not-received specimens HardStop create/link (`ABID-WORKUP-SPEC-UNUSABLE` / `ABID-WORKUP-SPEC-EXPIRED` / `ABID-WORKUP-SPEC-NOT-READY`). Received-not-accepted warns (`ABID-WORKUP-SPEC-UNACCEPTED`). Completing on a rejected, cancelled, or collected linked specimen HardStops; completing after the specimen expires or is still only received warns and requires acknowledgment. Completing while reserved or issued units remain warns (`ABID-COMPLETE-PRODUCTS`) and requires acknowledgment; it does not identify antibodies or release those units (`FRS-BB-194`). Dosage `CannotExclude` warns that a homozygous selected cell is needed (`ABID-SEL-CELL`). | URS-BB-063 | `AntibodyIdentificationAssistEvaluator`; `AntibodyIdentificationInterpretationRule`; `AntibodyIdentificationHistoryPostRule`; `AntibodyIdentificationService`; `ResultService` | `AntibodyIdentificationAssistEvaluatorTests`; `AntibodyIdentificationInterpretationRuleTests`; `AntibodyIdentificationHistoryPostRuleTests`; `AntibodyIdentificationWorkupTests`; `AntibodyIdentificationTests` |
| FRS-BB-098 | Workspace patient merge requires `patient.merge` in `PatientMergeService.MergeAsync` (not only the API). Inbound ADT A18/A40 uses `MergeFromHl7Async` and stays ungated. | URS-BB-064 | `PatientMergeService`; `PatientAuthorizationRule` | `PatientAuthorizationRuleTests`; PatientMergeService permission tests |
| FRS-BB-099 | Invalidating an unverified correction restores the prior verified row as current and marks the correction superseded by that row. The patient result panel lists only current rows (`SupersededByResultId` null) and expands retained prior versions with value, status, source, reason, user, and time. | URS-BB-065, URS-BB-004 | `ResultLifecycleRule.IsCurrentRow`; `ResultService.InvalidateResultAsync`; `PatientDetail.razor` | `ResultLifecycleRuleTests`; `ResultLifecycleTests.InvalidateUnverifiedCorrection_RestoresPriorVerified` |
| FRS-BB-100 | Workspace order create requires `patient.write` in `OrderService.CreateAsync` (not only the API). Inbound ORM uses `CreateFromHl7Async`. Allocation-created crossmatch orders use `CreateForAllocationAsync`. | URS-BB-066 | `OrderService`; `OrderAuthorizationRule` | `OrderAuthorizationRuleTests`; `OrderServiceAuthorizationTests` |
| FRS-BB-101 | Patient test history includes every result that has a `VerifiedUtc`, including superseded verified rows. Never-verified entries are omitted. Rows expose status, source, current flag, and correction/invalidation reason (from the successor when the prior row has none). | URS-BB-067, URS-BB-004 | `PatientTestHistoryService`; `PatientDetail.razor` test-history tab | `PatientTestHistoryTests` |
| FRS-BB-102 | Creating or updating a directory user, or assigning roles, requires `admin.users.manage` in `UserAdminService`. Creating or updating a role catalog entry requires `admin.roles.manage`. Callers that skip the API cannot grant `issue.create`. | URS-BB-068 | `UserAdminService`; `AdminAuthorizationRule` | `AdminAuthorizationRuleTests`; `UserAdminAuthorizationTests` |
| FRS-BB-103 | Direct `InterfaceTransfusionService.DocumentAsync` requires `transfusion.document`. Inbound BPAM uses `DocumentFromHl7Async` and stays ungated. | URS-BB-069 | `InterfaceTransfusionService`; `IssueAuthorizationRule` | `IssueAuthorizationRuleTests`; `InterfaceTransfusionAuthorizationTests` |
| FRS-BB-104 | Subtest, blood-attribute, and reflex-rule create/update write `AuditEventType.TestChange` (activate/deactivate remain Activate/Deactivate). | URS-BB-070, URS-BB-011 | `SubtestDefinitionAdminService`; `BloodAttributeAdminService`; `ReflexRuleAdminService` | `SubtestAndGrouperAdminTests`; `AdminCatalogTests`; `ReflexRulesTests` |
| FRS-BB-105 | Starting an ISBT scan session or adding a scan line requires `inventory.receive` in `ScanSessionService` (not only complete). Callers that skip the API cannot accumulate receive drafts. | URS-BB-071 | `ScanSessionService`; `InventoryAuthorizationRule` | `InventoryAuthorizationRuleTests`; `ScanSessionAuthorizationTests` |
| FRS-BB-106 | Unit ABO/Rh retype record writes `AuditEventType.Result`. Verify writes `Verify` on the retype row and `ProductStatus` on the unit (Received → Available or Quarantine). Interpreted ABO/Rh on those Result/Verify payloads is `FRS-BB-186`. | URS-BB-072, URS-BB-018, URS-BB-011 | `ProductRetypeService` | `ProductRetypeServiceTests.MatchingRetype_StaysReceivedUntilVerifiedBySecondUser` |
| FRS-BB-107 | Activating, deactivating, locking, unlocking, or requesting a password reset for a directory user requires `admin.users.manage` in `UserAdminService`. Callers that skip the API cannot disable a bench user or unlock an account. | URS-BB-073 | `UserAdminService`; `AdminAuthorizationRule` | `AdminAuthorizationRuleTests`; `UserAdminAuthorizationTests` |
| FRS-BB-108 | Creating, updating, activating, deactivating, or cloning a test definition requires `admin.tests.manage` in `TestDefinitionAdminService`. Callers that skip the API cannot change ABO interpretation or compatibility flags. | URS-BB-074 | `TestDefinitionAdminService`; `TestCatalogAuthorizationRule` | `TestCatalogAuthorizationRuleTests`; `TestDefinitionAuthorizationTests` |
| FRS-BB-109 | Exception-definition create/update write `AuditEventType.Configure`. Phase-definition create/update write `AuditEventType.TestChange`. Activate/deactivate remain Activate/Deactivate. | URS-BB-075, URS-BB-011 | `ExceptionDefinitionAdminService`; `PhaseDefinitionAdminService` | `AdminCatalogTests` exception and phase audit tests |
| FRS-BB-110 | Creating or updating a blood-attribute definition requires `admin.config.edit` in `BloodAttributeAdminService`. Activating or deactivating requires `admin.config.activate`. Callers that skip the API cannot change antigen-negative selection. | URS-BB-076 | `BloodAttributeAdminService`; `BloodAttributeAuthorizationRule` | `BloodAttributeAuthorizationRuleTests`; `BloodAttributeAuthorizationTests` |
| FRS-BB-111 | Order/test rule-definition create/update write `AuditEventType.Configure`. Test-grouper create/update write `AuditEventType.TestChange`. Activate/deactivate remain Activate/Deactivate. | URS-BB-077, URS-BB-011 | `RuleDefinitionAdminService`; `TestGrouperAdminService` | `RulesEngineTests` rule-admin audit assertions; `SubtestAndGrouperAdminTests` |
| FRS-BB-112 | Creating or updating a reflex rule requires `admin.tests.manage` in `ReflexRuleAdminService`. Activating or deactivating requires `admin.config.activate`. Callers that skip the API cannot change auto-ordered follow-up tests. | URS-BB-078 | `ReflexRuleAdminService`; `ReflexRuleAuthorizationRule` | `ReflexRuleAuthorizationRuleTests`; `ReflexRuleAuthorizationTests` |
| FRS-BB-113 | Creating or updating a subtest definition requires `admin.tests.manage` in `SubtestDefinitionAdminService`. Activating or deactivating requires `admin.config.activate`. Callers that skip the API cannot change panel interpretation choices. | URS-BB-079 | `SubtestDefinitionAdminService`; `SubtestCatalogAuthorizationRule` | `SubtestCatalogAuthorizationRuleTests`; `SubtestDefinitionAuthorizationTests` |
| FRS-BB-114 | Specimen-type create/update write `AuditEventType.TestChange`. Activate/deactivate remain Activate/Deactivate. | URS-BB-080, URS-BB-011 | `SpecimenTypeAdminService` | `AdminCatalogTests` specimen-type audit tests |
| FRS-BB-115 | Creating or updating a test grouper requires `admin.tests.manage` in `TestGrouperAdminService`. Activating or deactivating requires `admin.config.activate`. Callers that skip the API cannot drop a member test from Type and Screen. | URS-BB-081 | `TestGrouperAdminService`; `TestGrouperAuthorizationRule` | `TestGrouperAuthorizationRuleTests`; `TestGrouperAuthorizationTests` |
| FRS-BB-116 | HL7 endpoint create/update write `AuditEventType.Interface`. Enable/disable remain Activate/Deactivate. | URS-BB-082, URS-BB-011, URS-BB-013 | `Hl7ConfigAdminService` | `AdminInterfaceSetupTests` |
| FRS-BB-117 | Creating or updating an order or test rule requires `admin.tests.manage` in `RuleDefinitionAdminService`. Activating or deactivating requires `admin.config.activate`. Callers that skip the API cannot add or cancel follow-up tests. | URS-BB-083 | `RuleDefinitionAdminService`; `RuleDefinitionAuthorizationRule` | `RuleDefinitionAuthorizationRuleTests`; `RuleDefinitionAuthorizationTests` |
| FRS-BB-118 | Creating or updating a phase definition requires `admin.tests.manage` in `PhaseDefinitionAdminService`. Activating or deactivating requires `admin.config.activate`. Callers that skip the API cannot change result-entry phases or interpretation inclusion. | URS-BB-084 | `PhaseDefinitionAdminService`; `PhaseCatalogAuthorizationRule` | `PhaseCatalogAuthorizationRuleTests`; `PhaseDefinitionAuthorizationTests` |
| FRS-BB-119 | Compatibility-table version and rule create/update write `AuditEventType.Configure`. Activate/deactivate remain Activate/Deactivate. | URS-BB-085, URS-BB-011 | `CompatibilityRuleAdminService` | `CompatibilityRuleAdminTests` |
| FRS-BB-120 | Creating or updating an exception definition requires `admin.config.edit` in `ExceptionDefinitionAdminService`. Activating or deactivating requires `admin.config.activate`. Callers that skip the API cannot change which HardStops are overridable at result verify. | URS-BB-086 | `ExceptionDefinitionAdminService`; `ExceptionCatalogAuthorizationRule` | `ExceptionCatalogAuthorizationRuleTests`; `ExceptionDefinitionAuthorizationTests` |
| FRS-BB-121 | Special-requirement add writes `AuditEventType.Antibody` after save so the event has an entity id, type, antigen, and reason. Deactivate writes `Deactivate` with old/new. | URS-BB-087, URS-BB-011 | `SpecialRequirementService` | `Phase3ServicesTests.SpecialRequirement_AddAndDeactivate_WriteNamedAudits` |
| FRS-BB-122 | Creating or updating a specimen-type definition requires `admin.config.edit` in `SpecimenTypeAdminService`. Activating or deactivating requires `admin.config.activate`. Callers that skip the API cannot exclude a test from a specimen type. | URS-BB-088 | `SpecimenTypeAdminService`; `SpecimenTypeAuthorizationRule` | `SpecimenTypeAuthorizationRuleTests`; `SpecimenTypeAuthorizationTests` |
| FRS-BB-123 | Creating or updating a product definition requires `admin.products.manage` in `ProductAdminService`. Activating or deactivating requires `admin.config.activate`. Callers that skip the API cannot change which products require crossmatch, ABO/Rh match, or retype. | URS-BB-089 | `ProductAdminService`; `ProductCatalogAuthorizationRule` | `ProductCatalogAuthorizationRuleTests`; `ProductCatalogAuthorizationTests` |
| FRS-BB-124 | Test worklist items include `CurrentResultSource`. The global worklist, patient Tests tab, and specimen-linked test table display it. | URS-BB-090, URS-BB-047 | `TestWorklistService`; `TestWorkItemDto` | `TestWorklistTests.PendingWorklist_IncludesInstrumentSource` |
| FRS-BB-125 | Creating or updating a modification rule requires `admin.modification-rules.manage` in `ModificationRuleAdminService`. Activating or deactivating requires `admin.config.activate`. Callers that skip the API cannot change which source product, modification type, target product, or expiration offset is allowed. | URS-BB-091 | `ModificationRuleAdminService`; `ModificationRuleAuthorizationRule` | `ModificationRuleAuthorizationRuleTests`; `ModificationRuleAuthorizationTests` |
| FRS-BB-126 | Product-definition create/update write `AuditEventType.Configure`. Activate/deactivate remain Activate/Deactivate. | URS-BB-092, URS-BB-011, URS-BB-018 | `ProductAdminService` | `ProductCatalogAuthorizationTests.CreateAndUpdate_WriteConfigure` |
| FRS-BB-127 | Creating or updating an expiration modification code requires `admin.modification-rules.manage` in `ExpirationModificationCodeAdminService`. Activating or deactivating requires `admin.config.activate`. Callers that skip the API cannot change how long a modified unit remains usable. | URS-BB-093 | `ExpirationModificationCodeAdminService`; `ExpirationModificationCodeAuthorizationRule` | `ExpirationModificationCodeAuthorizationRuleTests`; `ExpirationModificationCodeAuthorizationTests` |
| FRS-BB-128 | Creating, updating, enabling, or disabling an HL7 endpoint requires `admin.hl7.manage` in `Hl7ConfigAdminService`. Callers that skip the API cannot add or enable an inbound results (ORU) endpoint. | URS-BB-094 | `Hl7ConfigAdminService`; `Hl7EndpointAuthorizationRule` | `Hl7EndpointAuthorizationRuleTests`; `Hl7EndpointAuthorizationTests` |
| FRS-BB-129 | Expiration-modification-code create/update write `AuditEventType.Configure`. Activate/deactivate remain Activate/Deactivate. | URS-BB-095, URS-BB-011, URS-BB-093 | `ExpirationModificationCodeAdminService` | `ExpirationModificationCodeAuthorizationTests.CreateAndUpdate_WriteConfigure` |
| FRS-BB-130 | Replacing HL7 value translations requires `admin.hl7.manage` in `InterfaceTranslationAdminService`. Callers that skip the API cannot change how inbound result values map to internal codes. | URS-BB-096 | `InterfaceTranslationAdminService`; `InterfaceTranslationAuthorizationRule` | `InterfaceTranslationAuthorizationRuleTests`; `InterfaceTranslationAuthorizationTests` |
| FRS-BB-131 | Modification-rule create/update write `AuditEventType.Configure`. Activate/deactivate remain Activate/Deactivate. | URS-BB-097, URS-BB-011, URS-BB-091 | `ModificationRuleAdminService` | `ModificationRuleAuthorizationTests.CreateAndUpdate_WriteConfigure` |
| FRS-BB-132 | Creating or updating a charge code requires `admin.config.edit` in `ChargeCodeAdminService`. Activating or deactivating requires `admin.config.activate`. Callers that skip the API cannot change which codes are billed after issue. | URS-BB-098 | `ChargeCodeAdminService`; `ChargeCodeAuthorizationRule` | `ChargeCodeAuthorizationRuleTests`; `ChargeCodeAuthorizationTests` |
| FRS-BB-133 | Creating or updating a charge rule requires `admin.config.edit` in `ChargeRuleAdminService`. Activating or deactivating requires `admin.config.activate`. Callers that skip the API cannot change which trigger bills after issue. | URS-BB-099 | `ChargeRuleAdminService`; `ChargeRuleAuthorizationRule` | `ChargeRuleAuthorizationRuleTests`; `ChargeRuleAuthorizationTests` |
| FRS-BB-135 | Creating or updating a product billing row requires `admin.config.edit` in `ProductBillingAdminService`. Activating or deactivating requires `admin.config.activate`. Callers that skip the API cannot change which ISBT product bills after issue. | URS-BB-101 | `ProductBillingAdminService`; `ProductBillingAuthorizationRule` | `ProductBillingAuthorizationRuleTests`; `ProductBillingAuthorizationTests` |
| FRS-BB-137 | Creating or updating a test/service billing row requires `admin.config.edit` in `TestServiceBillingAdminService`. Activating or deactivating requires `admin.config.activate`. Callers that skip the API cannot change which verified test bills after release. | URS-BB-103 | `TestServiceBillingAdminService`; `TestServiceBillingAuthorizationRule` | `TestServiceBillingAuthorizationRuleTests`; `TestServiceBillingAuthorizationTests` |
| FRS-BB-138 | Creating or updating an ordering provider requires `admin.config.edit` in `OrderingProviderAdminService`. Activating or deactivating requires `admin.config.activate`. Callers that skip the API cannot change who can be named on an order. | URS-BB-104 | `OrderingProviderAdminService`; `OrderingProviderAuthorizationRule` | `OrderingProviderAuthorizationRuleTests`; `OrderingProviderAuthorizationTests` |
| FRS-BB-140 | Creating or updating an ordering location requires `admin.config.edit` in `OrderingLocationAdminService`. Activating or deactivating requires `admin.config.activate`. Callers that skip the API cannot change which ward can be named on an order. | URS-BB-106 | `OrderingLocationAdminService`; `OrderingLocationAuthorizationRule` | `OrderingLocationAuthorizationRuleTests`; `OrderingLocationAuthorizationTests` |
| FRS-BB-142 | Reviewing a captured charge requires `billing.review` in `BillingService`. Cancelling requires `billing.cancel`. Exporting requires `billing.export`. Capture and the review queue stay ungated so clinical paths can still write pending events. | URS-BB-108 | `BillingService`; `BillingAuthorizationRule` | `BillingAuthorizationRuleTests`; `BillingAuthorizationTests` |
| FRS-BB-143 | Creating or updating an inventory location requires `admin.config.edit` in `InventoryLocationAdminService`. Activating or deactivating requires `admin.config.activate`. Callers that skip the API cannot change where units can be stored or issued. | URS-BB-109 | `InventoryLocationAdminService`; `InventoryLocationAuthorizationRule` | `InventoryLocationAuthorizationRuleTests`; `InventoryLocationAuthorizationTests` |
| FRS-BB-145 | Updating a facility policy requires `admin.config.edit` in `FacilityPolicyAdminService`. Callers that skip the API cannot change electronic XM, second ABO, uncrossmatched O, specimen windows, or other facility switches. List still materializes missing catalog defaults. | URS-BB-111 | `FacilityPolicyAdminService`; `FacilityPolicyAuthorizationRule` | `FacilityPolicyAuthorizationRuleTests`; `FacilityPolicyAuthorizationTests` |
| FRS-BB-147 | DIN lookback search and recipient traceback require `lookback.manage` in `LookbackService`. Callers that skip the API cannot list who received a donation. Recall and notification attempt stay separately gated. | URS-BB-113 | `LookbackService`; `LookbackAuthorizationRule` | `LookbackAuthorizationRuleTests`; `LookbackSearchAuthorizationTests` |
| FRS-BB-148 | Charge-rule create/update write `AuditEventType.Configure`. Activate/deactivate remain Activate/Deactivate. | URS-BB-114, URS-BB-011, URS-BB-099 | `ChargeRuleAdminService` | `ChargeRuleAuthorizationTests.CreateAndUpdate_WriteConfigure` |
| FRS-BB-150 | Test/service-billing create/update write `AuditEventType.Configure`. Activate/deactivate remain Activate/Deactivate. | URS-BB-116, URS-BB-011, URS-BB-103 | `TestServiceBillingAdminService` | `TestServiceBillingAuthorizationTests.CreateAndUpdate_WriteConfigure` |
| FRS-BB-152 | Ordering-location create/update write `AuditEventType.Configure`. Activate/deactivate remain Activate/Deactivate. | URS-BB-118, URS-BB-011, URS-BB-106 | `OrderingLocationAdminService` | `OrderingLocationAuthorizationTests.CreateAndUpdate_WriteConfigure` |
| FRS-BB-153 | Inventory-location create/update write `AuditEventType.Configure`. Activate/deactivate remain Activate/Deactivate. | URS-BB-119, URS-BB-011, URS-BB-109 | `InventoryLocationAdminService` | `InventoryLocationAuthorizationTests.CreateAndUpdate_WriteConfigure` |
| FRS-BB-154 | Charge-code create/update write `AuditEventType.Configure`. Activate/deactivate remain Activate/Deactivate. | URS-BB-120, URS-BB-011, URS-BB-098 | `ChargeCodeAdminService` | `ChargeCodeAuthorizationTests.CreateAndUpdate_WriteConfigure` |
| FRS-BB-155 | Product-billing create/update write `AuditEventType.Configure`. Activate/deactivate remain Activate/Deactivate. | URS-BB-121, URS-BB-011, URS-BB-101 | `ProductBillingAdminService` | `ProductBillingAuthorizationTests.CreateAndUpdate_WriteConfigure` |
| FRS-BB-156 | Ordering-provider create/update write `AuditEventType.Configure`. Activate/deactivate remain Activate/Deactivate. | URS-BB-122, URS-BB-011, URS-BB-104 | `OrderingProviderAdminService` | `OrderingProviderAuthorizationTests.CreateAndUpdate_WriteConfigure` |
| FRS-BB-157 | Directory user create/update and role-catalog create write `AuditEventType.UserRole`. Role assignment, role-catalog update, and activate/deactivate stay named as-is. Lock and password-reset write UserRole (`FRS-BB-170`). | URS-BB-123, URS-BB-011, URS-BB-068 | `UserAdminService` | `UserAdminAuthorizationTests.CreateAndUpdate_WriteUserRole` |
| FRS-BB-158 | Ward receipt of an issued unit writes `AuditEventType.Transfusion` on the issue (who/when/late), not generic Update. | URS-BB-124, URS-BB-011, URS-BB-040 | `IssuingService.RecordWardReceiptAsync` | `IssuingReturnAuthorizationTests.WardReceipt_WithoutTransfusionDocument_IsHardStopped` |
| FRS-BB-159 | Directed-to-allogeneic conversion writes `AuditEventType.ProductStatus` on the unit (old/new restriction, reserved patient, reason, second verifier), not generic Update. | URS-BB-125, URS-BB-011, URS-BB-024 | `InventoryService.ConvertDirectedToAllogeneicAsync` | `InventoryServiceTests.ConvertDirected_FromQuarantine_ClearsReservation` |
| FRS-BB-160 | Charge review and cancel write `AuditEventType.Billing` with old/new status (cancel includes the reason). Export writes `AuditEventType.Export`. Capture stays interceptor Create. | URS-BB-126, URS-BB-011, URS-BB-108 | `BillingService` | `BillingAuthorizationTests.ReviewAndCancel_WriteBilling`; `BillingAuthorizationTests.Export_WritesExport` |
| FRS-BB-161 | Patient demographic update writes `AuditEventType.PatientAccess` with old/new name, date of birth, sex, status, and pregnancy. Chart-open remains PatientAccess with reason "Patient record opened." Create stays interceptor Create. ADT inbound still bypasses `PatientService`. | URS-BB-127, URS-BB-011, URS-BB-031 | `PatientService.UpdateAsync` | `PatientServiceTests.Update_WritesPatientAccess` |
| FRS-BB-162 | Patient create writes `AuditEventType.PatientAccess` with MRN, name, date of birth, and sex. Chart-open and demographic update remain PatientAccess with distinct reasons. Interceptor Create still writes. ADT inbound still bypasses `PatientService`. | URS-BB-128, URS-BB-011, URS-BB-045 | `PatientService.CreateAsync` | `PatientServiceTests.Create_WritesPatientAccess` |
| FRS-BB-163 | Worklist and specimen panel entry forms state that catalog-matching panel entry is stored as `ResultSource.Calculated`, and save feedback names the stored source. Instrument and Interface keep their source. This is UX for `ResultProvenanceRule`, not a new regulatory claim. | URS-BB-129, URS-BB-047, URS-BB-056, URS-BB-090 | `ResultSourceEntryCopy`; `TestResultEntryPanel`; `PatientDetail` | `ResultSourceEntryCopyTests` |
| FRS-BB-164 | HL7 value-translation replace writes `AuditEventType.Interface` with old/new rows for that data item. Endpoint create/update remain Interface. Get stays ungated. | URS-BB-130, URS-BB-011, URS-BB-096, URS-BB-013 | `InterfaceTranslationAdminService` | `InterfaceTranslationAuthorizationTests.Replace_WritesInterface` |
| FRS-BB-165 | Workspace visit create/update write `AuditEventType.PatientAccess` on `Encounter` with visit/location/status old/new. ADT upsert and HL7 order-encounter ensure stay ungated and interceptor-only. | URS-BB-131, URS-BB-011, URS-BB-058 | `EncounterService` | `EncounterServiceAuthorizationTests.CreateAndUpdate_WritePatientAccess` |
| FRS-BB-166 | First specimen, compatibility, and component label prints write `AuditEventType.Print` after the job has an id. Reprint remains Reprint. Interceptor Create still writes. | URS-BB-132, URS-BB-011, URS-BB-055 | `PrintService.CreateJobAsync` | `Phase6PrintingTests.PrintSpecimenLabel_WritesPrint` |
| FRS-BB-167 | The specimen typed-entry form (test code + value, no panel) states that the stored source is Manual, and enter feedback names the stored source. Instrument and Interface keep their source. This is UX for `ResultProvenanceRule`, not a new regulatory claim. | URS-BB-133, URS-BB-047, URS-BB-090 | `ResultSourceEntryCopy`; `PatientDetail` | `ResultSourceEntryCopyTests` |
| FRS-BB-168 | Antibody-identification workup open, specimen link, lot attach, assist, interpret, void, and stale-panel withdrawal write `AuditEventType.Antibody` on the workup. Supervisor accept and complete remain Verify. Posted Identified findings remain Antibody on `AntibodyHistory`. History-row id on that Antibody event is `FRS-BB-188`. | URS-BB-134, URS-BB-011, URS-BB-063 | `AntibodyIdentificationService` | `AntibodyIdentificationWorkupTests.CreateWorkup_WritesAntibody` |
| FRS-BB-169 | Electronic XM eligibility HardStops with `XM-EC-ABID-OPEN` when a workup is InProgress, PendingInterpretation, or PendingSupervisorReview. Completed and Voided do not block. Serologic XM and emergency release stay available. | URS-BB-135, URS-BB-009 | `ElectronicCrossmatchEligibilityRule`; `ElectronicCrossmatchEligibilityService`; `CompatibilityService`; `IssuingService` | `CrossmatchRuleTests.ElectronicEligibility_OpenAntibodyIdWorkup_IsHardStop`; `ElectronicCrossmatchEligibilityTests.Assess_OpenAntibodyIdWorkup_BlocksEligibility` |
| FRS-BB-170 | Directory user lock, unlock, and password-reset request write `AuditEventType.UserRole`. Activate/deactivate remain Activate/Deactivate. Password reset remains a placeholder with no credential store. | URS-BB-136, URS-BB-011, URS-BB-073 | `UserAdminService.SetLockedAsync`; `UserAdminService.RequestPasswordResetAsync` | `UserAdminAuthorizationTests.LockAndPasswordReset_WriteUserRole` |
| FRS-BB-171 | Patient-chart antibody add and deactivate HardStop with `ABID-WORKUP-OPEN` when any workup is InProgress, PendingInterpretation, or PendingSupervisorReview. Chart add has no specimen, so any open workup on that patient applies (OCD-023). Completed and Voided do not block. Deactivate after complete or void stays the authorized immuno path (OCD-017). Free-text ABID verify remains separately gated. | URS-BB-137, URS-BB-002, URS-BB-063 | `AntibodyIdentificationHistoryPostRule.EvaluateManualHistoryAdd`; `EvaluateManualHistoryDeactivate`; `ImmunohematologyService` | `AntibodyIdentificationHistoryPostRuleTests`; `AntibodyIdentificationWorkupTests.AddAntibody_OpenWorkup_IsBlockedUntilVoided`; `DeactivateAntibody_OpenWorkup_IsBlockedUntilVoided` |
| FRS-BB-172 | Deviation create writes `AuditEventType.Deviation` after the row has an id. Status update writes Deviation with old/new status. Interceptor Create/Update still write. List stays ungated. | URS-BB-138, URS-BB-011, URS-BB-044 | `DeviationService` | `DeviationServiceTests.CreateAndUpdateStatus_WriteDeviation` |
| FRS-BB-173 | Reaction-investigation open writes `AuditEventType.ReactionInvestigation` after the row has an id. Workup update, CBER notification, and written-report timestamps write ReactionInvestigation with old/new. Opening from a suspected transfusion stays automatic and ungated. Interceptor Create/Update still write. | URS-BB-139, URS-BB-011, URS-BB-036 | `ReactionInvestigationService` | `ReactionInvestigationServiceTests.OpenAndUpdate_WriteReactionInvestigation` |
| FRS-BB-174 | DIN lookback recall writes `AuditEventType.Lookback` on a collected unit id with DIN/unit/notification counts. Notification-attempt recording writes Lookback on `LookbackNotification` with status/physician/notes old/new. Search and traceback remain Lookback. Interceptor Create/Update still write. The LIS does not auto-notify patients. | URS-BB-140, URS-BB-011, URS-BB-022, URS-BB-053 | `LookbackService` | `LookbackSearchAuthorizationTests.RecallAndRecordAttempt_WriteLookback` |
| FRS-BB-175 | Walk-in receive and ISBT normalized intake (scan-session complete or manual entry) write `AuditEventType.ProductStatus` on the unit after the row has an id (unit number, DIN, status, restriction, path). Expected packing-list create, arrival, and cancel write ProductStatus (`FRS-BB-176`). Discard and directed conversion remain separately named. | URS-BB-141, URS-BB-011, URS-BB-010, URS-BB-035, URS-BB-037 | `InventoryService.ReceiveUnitAsync`; `ReceiveNormalizedComponentAsync` | `InventoryServiceTests.ReceiveUnit_WritesProductStatus` |
| FRS-BB-176 | Expected inbound packing-list create writes `AuditEventType.ProductStatus` after the unit has an id. Arrival confirmation and cancel write ProductStatus with Expected old/new status. Interceptor Create/Update still write. Walk-in and ISBT receive remain `FRS-BB-175`. | URS-BB-142, URS-BB-011, URS-BB-010, URS-BB-037, URS-BB-051 | `InventoryService.ExpectUnitAsync`; `ReceiveExpectedUnitAsync`; `CancelExpectedUnitAsync` | `InventoryServiceTests.ExpectReceiveAndCancel_WriteProductStatus` |
| FRS-BB-177 | Submit-for-verification writes stored `ResultSource` on old/new (`Result`). Verify writes source on old and new. All three invalidate paths write source on old/new (`Invalidate`). Re-entry after invalidation writes prior and replacement source (`Result`, reason "Re-entry after invalidation."). Interface replacement after invalidate uses this path. Correct already stored source. Stored source is not changed. Interceptor Create/Update still write. | URS-BB-143, URS-BB-011, URS-BB-047, URS-BB-046 | `ResultService` | `ResultLifecycleTests.SubmitThenVerify_WritesVerifyAudit_WithOldAndNew`; `ResultLifecycleTests.InvalidateVerified_CreatesNewVersion_AndRetainsOriginal`; `ResultLifecycleTests.InvalidateUnverifiedCorrection_RestoresPriorVerified`; `ResultLifecycleTests.ReenterAfterInvalidate_WritesSourceOnOldAndNew` |
| FRS-BB-178 | Quarantine release and operational-hold release write `AuditEventType.ProductStatus` on the unit with Quarantine/OnHold old and Available new. Dual-verifier still applies on quarantine release. Placing into quarantine or hold is `FRS-BB-180`. Direct recall is `FRS-BB-179`. | URS-BB-144, URS-BB-011, URS-BB-010, URS-BB-023, URS-BB-025 | `InventoryService.ReleaseFromQuarantineAsync`; `ReleaseFromHoldAsync` | `InventoryServiceTests.ReleaseFromQuarantineAndHold_WriteProductStatus` |
| FRS-BB-179 | Direct inventory recall writes `AuditEventType.ProductStatus` on the unit with prior status old and Recalled new (unit number, reason, path). Lookback DIN recall remains Lookback (`FRS-BB-174`) and does not write this ProductStatus. Interceptor Update still writes. | URS-BB-145, URS-BB-011, URS-BB-010, URS-BB-043 | `InventoryService.RecallAsync` | `InventoryServiceTests.Recall_WritesProductStatus` |
| FRS-BB-180 | Manual place-into-quarantine and place-on-hold write `AuditEventType.ProductStatus` on the unit with prior status old and Quarantine/OnHold new (unit number, path, reason). Mark-missing and mark-damaged are `FRS-BB-181`. Locate and inspect are `FRS-BB-182`. Interceptor Update still writes. | URS-BB-146, URS-BB-011, URS-BB-010, URS-BB-060 | `InventoryService.QuarantineAsync`; `HoldAsync` | `InventoryServiceTests.QuarantineAndHold_WriteProductStatus` |
| FRS-BB-181 | Mark-missing and mark-damaged write `AuditEventType.ProductStatus` on the unit with prior status old and Missing/Damaged new (unit number, path, reason). Locate-after-missing and inspect-after-damage are `FRS-BB-182`. Interceptor Update still writes. | URS-BB-147, URS-BB-011, URS-BB-010, URS-BB-060 | `InventoryService.MarkMissingAsync`; `MarkDamagedAsync` | `InventoryServiceTests.MarkMissingAndDamaged_WriteProductStatus` |
| FRS-BB-182 | Locate-after-missing and inspect-after-damage write `AuditEventType.ProductStatus` on the unit with Missing/Damaged old and Quarantine new (unit number, path, prior reason). Units still enter Quarantine, not Available. Interceptor Update still writes. | URS-BB-148, URS-BB-011, URS-BB-010, URS-BB-050 | `InventoryService.LocateMissingAsync`; `InspectDamagedAsync` | `InventoryServiceTests.LocateAndInspect_WriteProductStatus` |
| FRS-BB-183 | Return to supplier writes `AuditEventType.ProductStatus` on the unit with prior status old and ReturnedToSupplier new (unit number, path, reason). Ward return and cancel-expected stay their existing named events. Interceptor Update still writes. | URS-BB-149, URS-BB-011, URS-BB-010, URS-BB-048 | `InventoryService.ReturnToSupplierAsync` | `InventoryServiceTests.ReturnToSupplier_WritesProductStatus` |
| FRS-BB-184 | Location transfer writes `AuditEventType.ProductStatus` on the unit with from/to location (unit number, unchanged status, path). Status history still records the same-status location move. Interceptor Update still writes. | URS-BB-150, URS-BB-011, URS-BB-010, URS-BB-042 | `InventoryService.TransferAsync` | `InventoryServiceTests.Transfer_WritesProductStatus` |
| FRS-BB-185 | The expiration sweep writes `AuditEventType.ProductStatus` on each unit that actually expires (prior status old, Expired new, unit number, path). Units the transition does not allow are skipped and are not named. The sweep does not discard. Interceptor Update still writes. | URS-BB-151, URS-BB-011, URS-BB-010, URS-BB-062 | `InventoryService.ExpireDueUnitsAsync` | `InventoryServiceTests.ExpireDueUnits_WritesProductStatus` |
| FRS-BB-186 | Unit ABO/Rh retype enter writes `AuditEventType.Result` with interpreted ABO/Rh. Update writes Result with old/new interpreted type. Verify writes interpreted ABO/Rh on old/new. ProductStatus on Available/Quarantine stays as-is. Stored type is not changed. Interceptor Create/Update still write. | URS-BB-152, URS-BB-011, URS-BB-072, URS-BB-018 | `ProductRetypeService` | `ProductRetypeServiceTests.RecordUpdateAndVerify_WriteInterpretedType` |
| FRS-BB-187 | Unit identity correction writes `AuditEventType.Correct` on the unit after the correction row has an id. Old/new include field, value, and `CorrectionId`. Post-issue lock stays. Interceptor Create still writes. | URS-BB-153, URS-BB-011, URS-BB-033 | `ComponentIdentityCorrectionService.CorrectAsync` | `ComponentIdentityCorrectionServiceTests.Correct_WritesCorrectWithCorrectionId` |
| FRS-BB-188 | Completing a reviewed antibody-identification workup posts Identified history then writes `AuditEventType.Antibody` on `AntibodyHistory` with the history row id, `PatientId`, `WorkupId`, and `HistoryId`. Dedup when the specificity is already active stays silent. Dual posting paths remain (OCD-023). Interceptor Create/Update still write. | URS-BB-154, URS-BB-011, URS-BB-063, URS-BB-134 | `AntibodyIdentificationService.PostHistoryAsync` | `AntibodyIdentificationWorkupTests.Complete_WritesAntibodyWithHistoryId` |
| FRS-BB-189 | Verified ABID (free-text/coded, `ContributesToAntibodyHistory`) posts Identified history then writes `AuditEventType.Antibody` on `AntibodyHistory` with the history row id, `PatientId`, `SourceResultId`, specificities, and `HistoryIds`. Workup-complete posting is `FRS-BB-188`. Dual posting paths remain (OCD-023). Interceptor Create/Update still write. | URS-BB-155, URS-BB-011, URS-BB-047 | `ResultService.ApplyAntibodyIdentificationResultAsync` | `AntibodyIdentificationTests.VerifyAbid_WritesAntibodyWithHistoryIds` |
| FRS-BB-190 | Unit retype save and verify feedback name the interpreted ABO/Rh. Available vs Quarantine wording stays. This is UX for the stored interpretation, not a new regulatory claim. | URS-BB-156, URS-BB-072 | `ProductRetypeEntryCopy`; `ProductRetypeEntryPanel` | `ProductRetypeEntryCopyTests` |
| FRS-BB-191 | Compatibility-page crossmatch save feedback names method and Compatible or Incompatible plus the row id. Named Crossmatch audit stays as-is. This is UX for the stored XM result, not a new regulatory claim. | URS-BB-157, URS-BB-047 | `CrossmatchEntryCopy`; `Compatibility` | `CrossmatchEntryCopyTests` |
| FRS-BB-192 | Worklist expand-to-enter XM save feedback names stored `ResultSource` plus method and Compatible or Incompatible. Non-XM save feedback stays source-only. This is UX for the stored XM result, not a new regulatory claim. | URS-BB-158, URS-BB-047, URS-BB-090 | `CrossmatchEntryCopy`; `TestResultEntryPanel` | `CrossmatchEntryCopyTests` |
| FRS-BB-193 | Allocate warns `ABID-ALLOC-OPEN` and still reserves. Serologic `RecordCrossmatchAsync` warns `ABID-XM-OPEN` and still records. Issue warns `ABID-ISSUE-OPEN` after the gate and still issues without an extra override. Electronic XM remains `XM-EC-ABID-OPEN`. Emergency release is unchanged. | URS-BB-159, URS-BB-009 | `AntibodyIdentificationHistoryPostRule`; `CompatibilityService`; `IssuingService` | `AntibodyIdentificationHistoryPostRuleTests`; `Phase4IssuingTests.Allocate_OpenAntibodyIdWorkup_WarnsAndReserves`; `RecordCrossmatch_OpenAntibodyIdWorkup_WarnsAndRecords`; `Issue_OpenAntibodyIdWorkup_WarnsAndIssues` |
| FRS-BB-194 | Completing a reviewed workup warns `ABID-COMPLETE-PRODUCTS` when the patient has a Reserved allocation or Issued unit. `ABID-COMPLETE-ACK` is required. Completing still posts Identified findings and does not identify antibodies or change product status. Dual posting paths remain (OCD-023). | URS-BB-160, URS-BB-063 | `AntibodyIdentificationInterpretationRule.EvaluateOpenProductsAtCompletion`; `AntibodyIdentificationService.CompleteAsync` | `AntibodyIdentificationInterpretationRuleTests.OpenProductsAtCompletion_ReservedOrIssued_Warns`; `AntibodyIdentificationWorkupTests.Complete_WithReservedUnit_WarnsAndRequiresAcknowledgment` |
| FRS-BB-195 | Voiding a workup warns `ABID-VOID-PRODUCTS` when the patient has a Reserved allocation or Issued unit. Void still succeeds, posts nothing, and does not require extra acknowledgment or change product status. Dual posting paths remain (OCD-023). | URS-BB-161, URS-BB-063 | `AntibodyIdentificationInterpretationRule.EvaluateOpenProductsAtVoid`; `AntibodyIdentificationService.VoidAsync` | `AntibodyIdentificationInterpretationRuleTests.OpenProductsAtVoid_ReservedOrIssued_Warns`; `AntibodyIdentificationWorkupTests.Void_WithReservedUnit_WarnsAndVoids` |
| FRS-BB-070 | Configuration catalogs are versioned/effective-dated where modeled and write `ConfigurationChangeHistory`. | URS-BB-012 | Admin services | Admin catalog tests |
| FRS-BB-080 | HL7 inbound stores raw message, parse status, ACK, and error-queue entries. ADT/ORM follow a merged MRN to the survivor and do not write demographics onto the loser. ADT A18/A40 apply <c>PatientMergeService</c> when configured. | URS-BB-013, URS-BB-015 | `Hl7InboundProcessor`; `PatientMergeFollow` | Phase 5 HL7 merge tests |
| FRS-BB-090 | Downtime recovery is documented; duplicate prevention uses unique keys listed in `docs/DOWNTIME_PLAN.md`. | URS-BB-014 | Downtime plan | Operational checklist |
