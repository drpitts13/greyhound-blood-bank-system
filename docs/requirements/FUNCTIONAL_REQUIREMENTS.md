# Functional requirements

| ID | Requirement | URS | Design | Tests |
|---|---|---|---|---|
| FRS-BB-001 | Patient identity match at issue requires two tokens that match stored MRN/DOB/name fields (`PatientIdentityMatchRule`). | URS-BB-001 | `IssueGate` `ISS-IDENTITY` | Domain + issue integration |
| FRS-BB-002 | Antibody history is append-only. Add requires `immuno.record`; deactivation requires a reason and `immuno.override` in the Application service. Deactivated rows remain visible and continue to block electronic XM. | URS-BB-002 | `AntibodyHistory`; `ImmunoAuthorizationRule`; `ElectronicCrossmatchEligibilityService` | `ElectronicXmHistoryRegressionTests`; Phase 3 antibody permission tests |
| FRS-BB-003 | Merged patient records HardStop accession, visits, orders, result entry/verify/correct/invalidate, immunohematology writes, special requirements, issue, allocation, compatibility evaluation, and crossmatch (`PAT-MERGED-INACTIVE`). Inactive status is not blocked. Staff with `patient.merge` can merge a duplicate into the survivor (`POST /api/patients/{id}/merge` and ADT A18/A40). Demographic `patient.write` does not grant merge. History is reassigned, not deleted. Antibody-identification workups are reassigned to the survivor; merge HardStops if both records have an open workup (`ABID-MERGE-DUP-OPEN`) and warns if one does (`ABID-MERGE-WORKUP`). Discordant ABO/Rh HardStops the merge. | URS-BB-015, URS-BB-017 | `PatientMergeService`; `PatientMergeRule`; patient detail merge UI | `PatientMergeServiceTests`; Phase 8 seed tests; Phase 5 HL7 merge tests; `MergedPatientClinicalUseTests` |
| FRS-BB-010 | Specimen expiration is computed from collection time and alloimmunization-risk policy. Expired or non-accepted specimens HardStop issue, result entry, and result verification. | URS-BB-003, URS-BB-016 | `SpecimenExpirationRule`; `ResultService.ValidateSpecimenForEntryAsync` | Domain expiry boundary; `MergedPatientClinicalUseTests` |
| FRS-BB-020 | Verified results are not overwritten. Corrections create a new version and retain the original. | URS-BB-004 | `ResultService` | Result versioning tests |
| FRS-BB-079 | Result provenance is stored as Manual, Instrument, Interface, or Calculated. Instrument and interface entries start `PendingVerification`. Manual and calculated entries start `Entered` and may be submitted for verification. | URS-BB-047 | `ResultSource`; `ResultLifecycleRule`; `ResultService` | `ResultLifecycleRuleTests`; `ResultLifecycleTests` |
| FRS-BB-089 | ABO/Rh panel entry and catalog-interpretation saves set `ResultSource.Calculated` unless the observation already arrived as Instrument or Interface. Test-definition create, update, and clone write `AuditEventType.TestChange` (activate/deactivate remain Activate/Deactivate). | URS-BB-047, URS-BB-056, URS-BB-011 | `ResultProvenanceRule`; `ResultService`; `TestDefinitionAdminService` | `ResultProvenanceRuleTests`; `ResultLifecycleTests`; `AdminConfigTests` |
| FRS-BB-082 | Inbound ORU posts through `ResultService.EnterFromInterfaceAsync`. Source is Interface; status is `PendingVerification`. Verified rows are not overwritten. The order must belong to the PID patient and have a linked accepted specimen. OBX-11 empty/F/C/P/R may post; other statuses NAK (OCD-019). | URS-BB-049 | `Hl7OruMapper`; `InterfaceResultAcceptanceRule`; `Hl7InboundProcessor` | `InterfaceResultAcceptanceRuleTests`; `Hl7MapperTests`; `Phase5Hl7Tests` inbound ORU |
| FRS-BB-077 | Invalidating a result requires `result.invalidate` and a reason. A verified result is superseded by a new Invalidated version; the original row stays Verified. An unverified correction is marked Invalidated and the prior verified row becomes current again (OCD-016). Posted ABO/antibody history is not auto-reverted (OCD-017). | URS-BB-046 | `ResultService.InvalidateResultAsync`; `ResultAuthorizationRule` | `ResultLifecycleTests`; `ResultAuthorizationRuleTests` |
| FRS-BB-021 | Manually recording current ABO/Rh requires `immuno.override` in the Application service (not only the API). Antibody add and antigen-profile writes require `immuno.record`. Antibody deactivate requires `immuno.override`. A reason is required for manual ABO and deactivate. History is append-only. Changing ABO/Rh while an antibody-identification workup is open warns (`ABID-ABO-OPEN`) and withdraws interpretation and review; the same type does not withdraw. Manual ABO after a completed unscoped workup warns (`ABID-ABO-DONE`) and does not reopen or retract posted history. | URS-BB-019, URS-BB-002 | `ImmunohematologyService`; `ImmunoAuthorizationRule`; `AntibodyIdentificationHistoryPostRule` | `ImmunoAuthorizationRuleTests`; Phase 3 immuno permission tests; `AntibodyIdentificationWorkupTests.RecordBloodType_OpenWorkup_WarnsAndWithdrawsInterpretation`; `AntibodyIdentificationWorkupTests.RecordBloodType_AfterCompletedUnscopedWorkup_WarnsAndDoesNotReopen` |
| FRS-BB-022 | Patient ABO/Rh verification applies `SelfVerifyRule` when `Result.BlockAboSelfVerify` is on (default). `MarkComplete` does not auto-verify ABO/Rh. Current `PatientBloodTypeHistory` is written only after a successful verify. A verified type change while an antibody-identification workup is open warns (`ABID-ABO-OPEN`) and withdraws interpretation and review; Retain and the same type do not withdraw. Correcting a verified ABO/Rh while a workup is open warns (`ABID-TYPE-CORR`) and does not withdraw; complete and supervisor Accept HardStop (`ABID-TYPE-PENDING`) until the correction is verified or invalidated. A type-changing ABO/Rh verify after a completed in-scope workup warns (`ABID-ABO-DONE`) and does not reopen or retract posted history. | URS-BB-020 | `ResultService`; `SelfVerifyRule`; `AntibodyIdentificationHistoryPostRule` | Phase 3 ABO self-verify and MarkComplete tests; `AntibodyIdentificationWorkupTests.VerifyAboRh_OpenWorkup_WarnsAndWithdrawsInterpretation`; `AntibodyIdentificationWorkupTests.VerifyAboRh_AfterCompletedWorkup_SameSpecimen_WarnsAndDoesNotReopen`; `AntibodyIdentificationWorkupTests.CorrectAntigen_OpenWorkup_WarnsAndDoesNotWithdraw` |
| FRS-BB-023 | Adding a special transfusion requirement requires `immuno.record` in the Application service. Deactivating one requires a reason and `immuno.override` and sets an end date. Callers that skip the API filter cannot change `ISS-SPECIAL-REQ` facts. Only clinically active rows (effective now, not end-dated) are listed as active and enforced. Adding or deactivating while an antibody-identification workup is open warns (`ABID-SR-OPEN`) and still records. Types come from the versioned `SpecialRequirementDefinitions` catalog (unit / issuing / patient). | URS-BB-021 | `SpecialRequirementService`; `SpecialRequirementAdminService`; `ImmunoAuthorizationRule`; `AntibodyIdentificationHistoryPostRule` | `ImmunoAuthorizationRuleTests`; Phase 3 special-requirement permission test; `AntibodyIdentificationWorkupTests.AddSpecialRequirement_OpenWorkup_WarnsAndAdds`; `SpecialRequirementAdminTests` |
| FRS-BB-024 | DIN lookback recall requires `lookback.manage` in the Application service. Each component recall is applied through `InventoryService.RecallForLookbackAsync`; a failed status transition fails the lookback. Selected/Assigned/Allocated/Crossmatched/ReturnPending may move to Recalled. Transfused units are notified, not recalled. | URS-BB-022 | `LookbackService`; `LookbackAuthorizationRule`; `InventoryStatusTransition` | `LookbackAuthorizationRuleTests`; `InventoryStatusTransitionTests`; Compliance lookback tests |
| FRS-BB-030 | Compatibility decisions return structured `RuleResult` codes and messages. The engine never downgrades a HardStop. | URS-BB-005 | `RuleEvaluation` | Aggregation tests |
| FRS-BB-031 | `IssueGate` evaluates identity, specimen, ABO/Rh, allocation, XM, special requirements, appearance, location, emergency ABO, antigen-negative, and autologous/directed rules before issue. | URS-BB-006 | `IssueGate` | `IssueGateSafetyRegressionTests` |
| FRS-BB-032 | Autologous/directed issue to a non-designated patient is HardStop `ISS-AUTO-DIR` inside the issue gate. | URS-BB-008 | `AutologousDirectedRule` | Domain + issue integration |
| FRS-BB-033 | Electronic XM eligibility is an evaluated service (`eligible` + criteria), not a checkbox. Any antibody-history row fails `XM-EC-HISTORY`. An open antibody-identification workup fails `XM-EC-ABID-OPEN`; an active extended-crossmatch special requirement fails `XM-EC-EXTXM`. Serologic XM remains available and warns (`ABID-XM-OPEN`) without blocking. | URS-BB-009 | `ElectronicCrossmatchEligibilityService`; `ElectronicCrossmatchEligibilityRule`; `AntibodyIdentificationHistoryPostRule` | Eligibility tests; `CrossmatchRuleTests`; `Phase4IssuingTests.RecordCrossmatch_OpenAntibodyIdWorkup_WarnsAndRecords` |
| FRS-BB-040 | Emergency/MTP issue is a distinct type. Missing XM becomes an overridable Warning; ABO/identity/status HardStops still apply unless a listed emergency warning conversion exists. Override reason and authorizer are required. Emergency/MTP also requires `issue.emergency-release`. Non-emergency warning overrides require `issue.override`. | URS-BB-006 | `IssuingService`; `IssueAuthorizationRule` | Phase 4 emergency tests; `IssueAuthorizationRuleTests`; Phase 8 seed tests |
| FRS-BB-041 | At most one `Reserved` allocation and one `Issued` issue exist per unit (filtered unique indexes). Concurrent conflicts fail closed. | URS-BB-007 | `IX_Allocations_OneReservedPerUnit`, `IX_Issues_OneOpenIssuePerUnit` | `AllocationIssueConcurrencyTests` |
| FRS-BB-050 | Inventory transitions are allow-listed. Each transition writes `InventoryStatusHistory` + audit. | URS-BB-010 | `InventoryStatusTransition` | Transition tests |
| FRS-BB-051 | Unit ABO/Rh retype is the second confirmation of the supplier type. Recording a matching retype (`result.enter`) moves Received → Available immediately. A mismatch moves Received → Quarantine. No second reviewer is required. | URS-BB-018 | `ProductRetypeService` | `ProductRetypeServiceTests` |
| FRS-BB-052 | Releasing a unit from quarantine requires `inventory.release` in the Application service (not only the API). Dual-verifier policy still applies. | URS-BB-023 | `InventoryService`; `InventoryAuthorizationRule` | `InventoryAuthorizationRuleTests`; InventoryService permission test |
| FRS-BB-053 | Converting a directed unit to allogeneic inventory requires `inventory.release` in the Application service (not only the API). Reason and dual-verifier policy still apply. | URS-BB-024 | `InventoryService`; `InventoryAuthorizationRule` | `InventoryAuthorizationRuleTests`; InventoryService directed-conversion permission test |
| FRS-BB-054 | Releasing a unit from operational hold requires `inventory.release` in the Application service (not only the API). | URS-BB-025 | `InventoryService`; `InventoryAuthorizationRule` | `InventoryAuthorizationRuleTests`; InventoryService hold-release permission test |
| FRS-BB-055 | Verifying a test result requires `result.verify` in the Application service (not only the API). Self-verify policy still applies to patient ABO/Rh. Unit retype confirmation is part of record (`result.enter`). A leftover Entered retype verify still requires `result.verify`. | URS-BB-026 | `ResultService`; `ProductRetypeService`; `ResultAuthorizationRule` | `ResultAuthorizationRuleTests`; Phase 3 and ProductRetype permission tests |
| FRS-BB-056 | Allocating a unit requires `compatibility.allocate` in the Application service. Recording a crossmatch requires `compatibility.crossmatch`. Callers that skip the API filter cannot reserve a unit or record XM used at issue. | URS-BB-027 | `CompatibilityService`; `CompatibilityAuthorizationRule` | `CompatibilityAuthorizationRuleTests`; PatientAllocation permission tests |
| FRS-BB-057 | Issuing a unit requires `issue.create` in the Application service (not only the API). Emergency/MTP still also requires `issue.emergency-release`. | URS-BB-028 | `IssuingService`; `IssueAuthorizationRule` | `IssueAuthorizationRuleTests`; Phase 4 issue-create permission test |
| FRS-BB-058 | Entering a result or unit retype requires `result.enter` in the Application service. Correcting a verified result requires `result.correct` (including save-as-correction). Invalidating a result requires `result.invalidate`. | URS-BB-029, URS-BB-046 | `ResultService`; `ProductRetypeService`; `ResultAuthorizationRule` | `ResultAuthorizationRuleTests`; Phase 3 and ProductRetype permission tests; `ResultLifecycleTests` |
| FRS-BB-059 | Accessioning a specimen requires `specimen.accession` in the Application service. Editing collection metadata requires `specimen.edit`. Rejecting a specimen requires `specimen.reject`. | URS-BB-030 | `SpecimenService`; `SpecimenAuthorizationRule` | `SpecimenAuthorizationRuleTests`; Phase 3 specimen permission tests |
| FRS-BB-061 | Updating patient demographics requires `patient.write` in the Application service (not only the API). ADT inbound still writes through the HL7 processor, not `PatientService`. | URS-BB-031 | `PatientService`; `PatientAuthorizationRule` | `PatientAuthorizationRuleTests`; PatientService permission test |
| FRS-BB-062 | Dividing, pooling, or applying a product modification requires `inventory.modify` in the Application service (not only the API). Result units still land in Quarantine. | URS-BB-032 | `BloodProductModificationService`; `InventoryAuthorizationRule` | `InventoryAuthorizationRuleTests`; BloodProductModificationService permission test |
| FRS-BB-063 | Correcting unit ISBT identity requires `inventory.correct-identity` in the Application service (not only the API). Post-issue corrections remain locked unless the post-event path is used. | URS-BB-033 | `ComponentIdentityCorrectionService`; `InventoryAuthorizationRule` | `InventoryAuthorizationRuleTests`; ComponentIdentityCorrectionService permission test |
| FRS-BB-064 | Releasing an allocation requires `compatibility.allocate` in the Application service (not only the API). Callers that skip the API filter cannot return a reserved unit to Available. | URS-BB-034 | `CompatibilityService`; `CompatibilityAuthorizationRule` | `CompatibilityAuthorizationRuleTests`; PatientAllocation release permission test |
| FRS-BB-065 | Completing an ISBT scan session or creating a manual component requires `inventory.receive` in the Application service (not only the API). Walk-in `InventoryService` receive remains a separate path. | URS-BB-035 | `ScanSessionService`; `ManualComponentEntryService`; `InventoryAuthorizationRule` | `InventoryAuthorizationRuleTests`; ISBT manual-entry permission test |
| FRS-BB-066 | Updating a reaction investigation, recording CBER notification, or recording the written fatality report requires `reaction.investigate` in the Application service. Opening from a suspected transfusion remains an automatic issue-path write. | URS-BB-036 | `ReactionInvestigationService`; `ReactionAuthorizationRule` | `ReactionAuthorizationRuleTests`; ReactionInvestigationService permission test |
| FRS-BB-067 | Walk-in receive, expected-arrival confirmation, and normalized-component intake require `inventory.receive` in `InventoryService` (not only the API). ISBT scan-session complete and manual entry remain separately gated. | URS-BB-037 | `InventoryService`; `InventoryAuthorizationRule` | `InventoryAuthorizationRuleTests`; InventoryService receive permission test |
| FRS-BB-068 | Returning an issued unit to Available, Quarantine, or Discarded requires `issue.return` in the Application service (not only the API). | URS-BB-038 | `IssuingService`; `IssueAuthorizationRule` | `IssueAuthorizationRuleTests`; Issuing return permission test |
| FRS-BB-069 | Documenting a transfusion requires `transfusion.document` in the Application service (not only the API). Callers that skip the API filter cannot mark a unit Transfused. | URS-BB-039 | `IssuingService`; `IssueAuthorizationRule` | `IssueAuthorizationRuleTests`; Issuing transfusion-document permission test |
| FRS-BB-071 | Recording ward receipt of an issued unit requires `transfusion.document` in the Application service (not only the API). Callers that skip the API filter cannot unblock transfusion documentation. | URS-BB-040 | `IssuingService`; `IssueAuthorizationRule` | `IssueAuthorizationRuleTests`; Issuing ward-receipt permission test |
| FRS-BB-072 | Discarding a unit requires `inventory.discard` in the Application service (not only the API). Dual-verifier checks still apply. | URS-BB-041 | `InventoryService`; `InventoryAuthorizationRule` | `InventoryAuthorizationRuleTests`; InventoryService discard permission test |
| FRS-BB-073 | Transferring a unit between locations requires `inventory.transfer` in the Application service (not only the API). | URS-BB-042 | `InventoryService`; `InventoryAuthorizationRule` | `InventoryAuthorizationRuleTests`; InventoryService transfer permission test |
| FRS-BB-074 | Direct inventory recall requires `inventory.recall` in the Application service. Lookback DIN recall remains `lookback.manage` only (OCD-014) via `RecallForLookbackAsync`. | URS-BB-043 | `InventoryService`; `LookbackService`; `InventoryAuthorizationRule` | `InventoryAuthorizationRuleTests`; InventoryService recall permission tests |
| FRS-BB-075 | Creating or updating a quality-system deviation requires `deviation.manage` in the Application service (not only the API). | URS-BB-044 | `DeviationService`; `DeviationAuthorizationRule` | `DeviationAuthorizationRuleTests`; DeviationService permission tests |
| FRS-BB-076 | Creating a patient record requires `patient.write` in `PatientService` (not only the API). ADT inbound still writes through the HL7 processor, not `PatientService`. | URS-BB-045 | `PatientService`; `PatientAuthorizationRule` | `PatientAuthorizationRuleTests`; PatientService create permission test |
| FRS-BB-077 | Saving a unit blood attribute requires `inventory.receive` in `InventoryService` (not only the API). | URS-BB-046 | `InventoryService`; `InventoryAuthorizationRule` | `InventoryAuthorizationRuleTests`; InventoryService attribute permission test |
| FRS-BB-081 | Returning a unit to the supplier requires `inventory.receive` in `InventoryService` (not only the API). | URS-BB-048 | `InventoryService`; `InventoryAuthorizationRule` | `InventoryAuthorizationRuleTests`; InventoryService return-to-supplier permission test |
| FRS-BB-083 | Locating a missing unit or inspecting a damaged unit into quarantine requires `inventory.release` in `InventoryService` (not only the API). | URS-BB-050 | `InventoryService`; `InventoryAuthorizationRule` | `InventoryAuthorizationRuleTests`; InventoryService locate/inspect permission tests |
| FRS-BB-084 | Recording or cancelling an expected inbound packing-list unit requires `inventory.receive` in `InventoryService` (not only the API). Arrival confirmation remains separately gated. | URS-BB-051 | `InventoryService`; `InventoryAuthorizationRule` | `InventoryAuthorizationRuleTests`; InventoryService expect/cancel permission tests |
| FRS-BB-060 | Audit events are append-only and written in the same save pipeline as the change. Named events capture who/what/when/where (workstation)/old/new/why when applicable. | URS-BB-011 | `BloodBankDbContext`; `IAuditWriter` | Audit tests |
| FRS-BB-078 | Result enter, in-place update, submit-for-verification, verify, correct, and invalidate write named audit events. Opening a patient chart (`GET /api/patients/{id}`) writes `PatientAccess`. List/search does not. | URS-BB-011 | `ResultService`; `PatientService.RecordAccessAsync` | `ResultLifecycleTests` |
| FRS-BB-085 | Specimen accession/edit/reject, order create/cancel/link, allocation, crossmatch, emergency/MTP issue, transfusion documentation, role assignment, and role-permission edits write named `AuditEventType` values (`Specimen`, `OrderChange`, `Assignment`, `Crossmatch`, `EmergencyRelease`, `Transfusion`, `UserRole`) instead of generic Create/Update/Issue. | URS-BB-052 | `SpecimenService`; `OrderService`; `CompatibilityService`; `IssuingService`; `UserAdminService` | Phase 3/4/8/10 named-audit assertions |
| FRS-BB-086 | Recording a lookback notification attempt requires `lookback.manage` in the Application service (not only the API). Find/traceback are gated separately (FRS-BB-147). | URS-BB-053 | `LookbackService`; `LookbackAuthorizationRule` | `LookbackAuthorizationRuleTests`; Compliance lookback attempt permission test |
| FRS-BB-087 | `GET /api/audit-events` filters by entity type/id, named `eventType`, user, and UTC window. The audit page exposes those filters and displays old/new JSON. Unknown event types return 400. There is still no update/delete path. | URS-BB-054, URS-BB-011 | `AuditTrailQuery`; `AuditEndpoints`; `Audit.razor` | `AuditTrailQueryTests`; `AuditPipelineTests` named-event query |
| FRS-BB-088 | Printing a specimen, compatibility, or component label requires `print.label` in `PrintService`. Reprinting a stored job still requires a reason and requires `print.reprint` in the service (not only the API). | URS-BB-055 | `PrintService`; `PrintAuthorizationRule` | `PrintAuthorizationRuleTests`; Phase 6 print permission tests |
| FRS-BB-091 | Updating or cancelling an order, or linking a specimen to an order, requires `patient.write` in `OrderService` (not only the API). Order create remains ungated in the service so inbound ORM can post. Create-time specimen association uses the same core link path without a second privilege check. | URS-BB-057 | `OrderService`; `OrderAuthorizationRule` | `OrderAuthorizationRuleTests`; `OrderServiceAuthorizationTests` |
| FRS-BB-092 | Creating or updating a visit from the workspace requires `patient.write` in `EncounterService` (not only the API). Inbound ADT visit upsert and HL7 order-encounter ensure remain ungated. | URS-BB-058 | `EncounterService`; `EncounterAuthorizationRule` | `EncounterAuthorizationRuleTests`; `EncounterServiceAuthorizationTests` |
| FRS-BB-093 | Manual ABO/Rh history writes `AuditEventType.Override`. Antibody add and deactivate write `AuditEventType.Antibody` (same named type as verified ABID posting). | URS-BB-059, URS-BB-011, URS-BB-019 | `ImmunohematologyService` | Phase 3 `ManualBloodType_RequiresReason_AndWritesAudit`; `Antibody_AddThenDeactivate_RequiresReason` |
| FRS-BB-094 | Manual quarantine, operational hold, mark-missing, and mark-damaged require `inventory.release` in `InventoryService` (not only the API). Locate and inspect remain separately gated. | URS-BB-060 | `InventoryService`; `InventoryAuthorizationRule` | `InventoryAuthorizationRuleTests`; InventoryService quarantine/hold/missing/damaged permission tests |
| FRS-BB-095 | Antigen-profile save/update writes `AuditEventType.Antibody` with old/new (in-place overwrite, OCD-022). A phenotype change while an antibody-identification workup is open warns (`ABID-AG-OPEN`) and withdraws interpretation and review, including a verified AGTYPE result that changes the stored type. Correcting a verified antigen result while a workup is open warns (`ABID-TYPE-CORR`) and does not withdraw; complete and supervisor Accept HardStop (`ABID-TYPE-PENDING`) until the correction is verified or invalidated. A type-changing antigen verify after a completed same-specimen workup, a chart antigen save after a completed unscoped workup, or an antigen-positive type that conflicts with posted Identified history, warns (`ABID-AG-DONE`) and does not reopen or retract posted history. Reflex-added order lines from verified results write `AuditEventType.OrderChange` instead of Create. | URS-BB-061, URS-BB-011 | `ImmunohematologyService.SaveAntigenProfileAsync`; `ResultService.VerifyResultAsync`; `ResultService.CorrectResultAsync` | Phase 3 antigen named-audit test; `AntibodyIdentificationWorkupTests.SaveAntigen_OpenWorkup_WarnsAndWithdrawsInterpretation`; `AntibodyIdentificationWorkupTests.VerifyAntigen_OpenWorkup_WarnsAndWithdrawsInterpretation`; `AntibodyIdentificationWorkupTests.CorrectAntigen_OpenWorkup_WarnsAndDoesNotWithdraw`; `AntibodyIdentificationWorkupTests.VerifyAntigen_AfterCompletedWorkup_SameSpecimen_WarnsAndDoesNotReopen`; `AntibodyIdentificationWorkupTests.SaveAntigen_AfterCompletedUnscopedWorkup_WarnsAndDoesNotReopen`; `ReflexRulesTests.VerifyPositiveAbsc_AddsAbidLine_AndShowsOnWorklist` |
| FRS-BB-096 | The expiration sweep requires `inventory.discard` in `InventoryService` (not only the API). Due units still move to Expired; the sweep does not discard. | URS-BB-062 | `InventoryService.ExpireDueUnitsAsync`; `InventoryAuthorizationRule` | `InventoryAuthorizationRuleTests`; InventoryService expire permission test |
| FRS-BB-097 | Antibody-identification workups record manufacturer/lot/expiration, cells, antigen profiles, phases, strengths, autocontrol, DAT, selected cells, comments, assist findings, technologist interpretation, and supervisor review. Assist may emit Excluded/Possible/CannotExclude/Historical only — never Identified. Saving reactions, lots, DAT, or specimen scope refreshes stored assistance to the current antigram without identifying or posting. Completion HardStops without interpretation, on assist-Identified findings, and (default) without distinct supervisor acceptance. Changing panel reactions, selected-cell lots, DAT, specimen scope, ABO/Rh, or antigen type after interpretation withdraws interpretation and review (`ABID-INTERP-STALE` / `ABID-REVIEW-STALE`) and HardStops complete until both are repeated. The open workup names that withdrawal (panel, specimen, ABO/Rh, or antigen-type). Completing with Identified findings while a panel or selected cell lacks an interpretive-phase reaction HardStops (`ABID-INCOMPLETE-RXN`). Completing re-evaluates assistance and warns on leftover `CannotExclude` specificities (`ABID-UNEXCLUDED`) and homozygous selected-cell needs (`ABID-SEL-CELL`) without identifying them. Completing with no Identified findings warns (`ABID-COMPLETE-NONE`) and posts nothing. Clinical completion warnings require a technologist acknowledgment (`ABID-COMPLETE-ACK`) before history posts; acknowledgment does not identify antibodies. Completing while reserved or issued units remain on the patient warns (`ABID-COMPLETE-PRODUCTS`) and requires that acknowledgment; it does not identify antibodies or release the units. Voiding while reserved or issued units remain warns (`ABID-VOID-PRODUCTS`) and still voids without posting. Supervisor Accept HardStops Identified sign-off on an incomplete panel or a rejected/cancelled specimen and requires acknowledgment (`ABID-REVIEW-ACK`) for the same clinical warnings. Autocontrol reactive with DAT not performed warns (`ABID-DAT-INDICATED`) at complete and does not HardStop. History is posted only from technologist Identified findings after review. Identified specificities are resolved to the blood-attribute catalog when unique so they drive antigen-negative selection; unmatched Identified specificities warn and post as free-text. Identifying a catalog specificity when the patient is antigen-positive warns (`ABID-INTERP-PHENO` / `ABID-INTERP-GENO`) and does not HardStop. Identifying a specificity that assistance would exclude warns (`ABID-INTERP-EXCLUDED`) at interpret and complete and HardStops interpret without a rationale (`ABID-INTERP-EXCL-REASON`). Interpretation also warns on leftover `CannotExclude` (`ABID-UNEXCLUDED`), selected-cell need, history remaining, and incomplete reactions without identifying them. The patient Antibodies card, the patient workup list, and the `/antibody-id` open worklist show specimen accession or Unscoped, with patient MRN on the open worklist, flag withdrawn or clock-expired attached lots (`HasInactiveLot` / `HasExpiredLot`, with `InactiveLotNumbers` / `ExpiredLotNumbers`) including selected-cell reagents, list `AttachedLotNumbers` so a selected-cell lot is visible and filterable, expose manufacturer name/code (`AttachedManufacturers`, `/antibody-id?manufacturer=`) so a selected-cell vendor is findable, and show manufacturer on selected-cell pickers and the named inactive/expired lot on the open workup. The open worklist and dashboard summary show the next bench action (`NextAction`: record reactions, interpret, or supervisor review). The patient workup list shows Completed or Voided when `NextAction` is `None`. Dashboard names withdrawn or expired lots (`InactiveLotNumbers` / `ExpiredLotNumbers`) without identifying antibodies. Completing with active antibody history warns (`ABID-HIST-REMAINS`) and does not remove history; a current exclusion of a historical specificity warns (`ABID-HIST-UNDETECTED`). An open in-scope workup HardStops free-text ABID verify or a blood-attribute antibody verify that would post or deactivate history; a completed in-scope workup skips free-text or blood-attribute antibody history changes and warns (`ABID-WORKUP-AUTHORITATIVE` / `ABID-WORKUP-DISAGREE`). Rejecting a linked specimen warns (`ABID-SPEC-OPEN`), withdraws interpretation and review, still rejects, and keeps the open workup as the patient-wide identification of record until void or re-link. Opening another workup while that rejected or cancelled link remains HardStops (`ABID-WORKUP-DUP-OPEN`); re-linking the same workup is allowed. The open worklist, Antibodies card, and dashboard flag those unusable specimens (`HasUnusableSpecimen`, `UnusableSpecimenCount`). A clock-expired or expired-status linked specimen is also patient-wide for history post and new workups (`HasExpiredSpecimen`, `ExpiredSpecimenCount`); complete remains allowed with acknowledgment. A Received (not yet accepted) linked specimen is likewise patient-wide (`HasUnacceptedSpecimen`, `UnacceptedSpecimenCount`, OCD-031); complete remains Warning + acknowledgment (OCD-026). A Collected (not yet received) linked specimen is likewise patient-wide (`HasNotReadySpecimen`, `NotReadySpecimenCount`, OCD-032); complete and Accept remain HardStop (OCD-026). Dashboard and worklist specimen-attention badges open `/antibody-id?unusable=1`, `/antibody-id?expired=1`, `/antibody-id?unaccepted=1`, or `/antibody-id?notready=1` (`MatchesSpecimenAttention`). Next-action counts open `/antibody-id?action=reactions`, `?action=interpret`, or `?action=review` (`MatchesNextAction`). Dashboard inactive/expired lot counts and the worklist lot-attention count open `/antibody-id?lotAttention=1` (`MatchesLotAttention`); named lot badges still open `/antibody-id?lot=`. Withdrawn-judgment counts open `/antibody-id?withdrawn=1` (`MatchesWithdrawnJudgment`, `HasWithdrawnJudgment`). Pending type-correction counts open `/antibody-id?typePending=1` (`HasPendingTypeCorrection`, OCD-029). Reserved or issued unit counts open `/antibody-id?products=1` (`HasReservedOrIssuedUnits`); complete still warns and requires acknowledgment (`ABID-COMPLETE-PRODUCTS`). Editing collection or expiration warns (`ABID-SPEC-EDIT`) and withdraws interpretation. Adding or deactivating antibody history from the patient chart HardStops while any open workup exists for that patient (`ABID-WORKUP-OPEN`); the Antibodies card disables Add antibody and Deactivate. Deactivating a specificity posted by a completed workup warns (`ABID-DEACT-POSTED`) and still deactivates. Changing a patient antigen while a workup is open — chart save or a verified antigen result — warns (`ABID-AG-OPEN`), still records, and withdraws interpretation and review so they are repeated against the current type. Invalidating that verified antigen result warns (`ABID-AG-INVAL`) and withdraws again; phenotype is not auto-reverted. Manually changing ABO/Rh, or verifying an ABO/Rh result that changes current type, while a workup is open warns (`ABID-ABO-OPEN`), still records, and withdraws interpretation and review. Invalidating that verified ABO/Rh result warns (`ABID-ABO-INVAL`) and withdraws again; posted blood-type history is not auto-reverted. Correcting a verified ABO/Rh or antigen result while a workup is open warns (`ABID-TYPE-CORR`) and does not withdraw; complete and supervisor Accept HardStop (`ABID-TYPE-PENDING`) until it is verified or invalidated. A scoped workup HardStops only for a pending correction on the linked specimen. Changing antigen or ABO/Rh after a completed in-scope workup warns (`ABID-AG-DONE` / `ABID-ABO-DONE`) and does not reopen or retract posted history (same-specimen verify; chart save when the completed workup was unscoped, or when a posted Identified specificity conflicts with a new antigen-positive type). Adding or deactivating a special requirement while a workup is open warns (`ABID-SR-OPEN`) and still records. Allocating a unit while a workup is open warns (`ABID-ALLOC-OPEN`) and still reserves. Serologic XM warns (`ABID-XM-OPEN`) and still records. Issue warns (`ABID-ISSUE-OPEN`) after the safety gate and still issues without an extra override. Void requires a reason, posts nothing, and is blocked after complete. Expired/inactive lots HardStop new workups and additional lot attach. A deactivated attached lot HardStops Accept and complete; a clock-expired attached lot warns and requires acknowledgment. Designated staff create manufacturers (`ABID-MFG-CREATE-PERM`) and lots with cells and typed antigens (`ABID-LOT-CREATE-PERM`); create does not identify antibodies. Staff can review a lot's cell/antigen map and see open workups and completed identifications of record that used the lot or manufacturer (including patient MRN, posted-history count, and list-row `PostedHistoryWorkupCount`) without identifying antibodies. Voided workups are omitted. Staff can open the `/antibody-id` worklist filtered by lot (`?lot=`). A deactivated manufacturer cannot be used for a new lot. Designated staff deactivate or restore lots (`ABID-LOT-DEACT-PERM` / `ABID-LOT-ACT-PERM`); deactivation requires a reason and does not identify antibodies. Opening without a specimen warns (`ABID-WORKUP-UNSCOPED`). An open workup can link a specimen; completed or voided workups HardStop (`ABID-WORKUP-SPECIMEN`). A second overlapping open workup HardStops (`ABID-WORKUP-DUP-OPEN`). Rejected, cancelled, expired, or collected-not-received specimens HardStop create/link (`ABID-WORKUP-SPEC-UNUSABLE` / `ABID-WORKUP-SPEC-EXPIRED` / `ABID-WORKUP-SPEC-NOT-READY`). Received-not-accepted warns (`ABID-WORKUP-SPEC-UNACCEPTED`). Completing on a rejected, cancelled, or collected linked specimen HardStops; completing after the specimen expires or is still only received warns and requires acknowledgment. Dosage `CannotExclude` warns that a homozygous selected cell is needed (`ABID-SEL-CELL`). | URS-BB-063 | `AntibodyIdentificationAssistEvaluator`; `AntibodyIdentificationInterpretationRule`; `AntibodyIdentificationHistoryPostRule`; `AntibodyIdentificationService`; `ResultService` | `AntibodyIdentificationAssistEvaluatorTests`; `AntibodyIdentificationInterpretationRuleTests`; `AntibodyIdentificationHistoryPostRuleTests`; `AntibodyIdentificationWorkupTests`; `AntibodyIdentificationTests` |
| FRS-BB-098 | Workspace patient merge requires `patient.merge` in `PatientMergeService.MergeAsync` (not only the API). Inbound ADT A18/A40 uses `MergeFromHl7Async` and stays ungated. | URS-BB-064 | `PatientMergeService`; `PatientAuthorizationRule` | `PatientAuthorizationRuleTests`; PatientMergeService permission tests |
| FRS-BB-099 | Invalidating an unverified correction restores the prior verified row as current and marks the correction superseded by that row. The patient result panel lists only current rows (`SupersededByResultId` null) and expands retained prior versions with value, status, source, reason, user, and time. | URS-BB-065, URS-BB-004 | `ResultLifecycleRule.IsCurrentRow`; `ResultService.InvalidateResultAsync`; `PatientDetail.razor` | `ResultLifecycleRuleTests`; `ResultLifecycleTests.InvalidateUnverifiedCorrection_RestoresPriorVerified` |
| FRS-BB-100 | Workspace order create requires `patient.write` in `OrderService.CreateAsync` (not only the API). Inbound ORM uses `CreateFromHl7Async`. Allocation-created crossmatch orders use `CreateForAllocationAsync`. | URS-BB-066 | `OrderService`; `OrderAuthorizationRule` | `OrderAuthorizationRuleTests`; `OrderServiceAuthorizationTests` |
| FRS-BB-101 | Patient test history includes every result that has a `VerifiedUtc`, including superseded verified rows. Never-verified entries are omitted. Rows expose status, source, current flag, and correction/invalidation reason (from the successor when the prior row has none). | URS-BB-067, URS-BB-004 | `PatientTestHistoryService`; `PatientDetail.razor` test-history tab | `PatientTestHistoryTests` |
| FRS-BB-102 | Creating or updating a directory user, or assigning roles, requires `admin.users.manage` in `UserAdminService`. Creating or updating a role catalog entry requires `admin.roles.manage`. Callers that skip the API cannot grant `issue.create`. | URS-BB-068 | `UserAdminService`; `AdminAuthorizationRule` | `AdminAuthorizationRuleTests`; `UserAdminAuthorizationTests` |
| FRS-BB-103 | Direct `InterfaceTransfusionService.DocumentAsync` requires `transfusion.document`. Inbound BPAM uses `DocumentFromHl7Async` and stays ungated. | URS-BB-069 | `InterfaceTransfusionService`; `IssueAuthorizationRule` | `IssueAuthorizationRuleTests`; `InterfaceTransfusionAuthorizationTests` |
| FRS-BB-104 | Subtest, blood-attribute, and reflex-rule create/update write `AuditEventType.TestChange` (activate/deactivate remain Activate/Deactivate). | URS-BB-070, URS-BB-011 | `SubtestDefinitionAdminService`; `BloodAttributeAdminService`; `ReflexRuleAdminService` | `SubtestAndGrouperAdminTests`; `AdminCatalogTests`; `ReflexRulesTests` |
| FRS-BB-105 | Starting an ISBT scan session or adding a scan line requires `inventory.receive` in `ScanSessionService` (not only complete). Callers that skip the API cannot accumulate receive drafts. | URS-BB-071 | `ScanSessionService`; `InventoryAuthorizationRule` | `InventoryAuthorizationRuleTests`; `ScanSessionAuthorizationTests` |
| FRS-BB-106 | Unit ABO/Rh retype record writes `AuditEventType.Result`, then `Verify` on the retype row and `ProductStatus` on the unit (Received → Available or Quarantine) in the same action. | URS-BB-072, URS-BB-018, URS-BB-011 | `ProductRetypeService` | `ProductRetypeServiceTests.MatchingRetype_ReleasesUnitWithoutSecondReviewer` |
| FRS-BB-107 | Activating, deactivating, locking, unlocking, or requesting a password reset for a directory user requires `admin.users.manage` in `UserAdminService`. Callers that skip the API cannot disable a bench user or unlock an account. Deactivate and lock also revoke outstanding auth sessions (FRS-BB-181). | URS-BB-073 | `UserAdminService`; `AdminAuthorizationRule` | `AdminAuthorizationRuleTests`; `UserAdminAuthorizationTests`; `AuthSessionServiceTests.UserAdminLock_RevokesOutstandingSessions` |
| FRS-BB-108 | Creating, updating, activating, deactivating, or cloning a test definition requires `admin.tests.manage` in `TestDefinitionAdminService`. Callers that skip the API cannot change ABO interpretation or compatibility flags. | URS-BB-074 | `TestDefinitionAdminService`; `TestCatalogAuthorizationRule` | `TestCatalogAuthorizationRuleTests`; `TestDefinitionAuthorizationTests` |
| FRS-BB-109 | Exception-definition create/update write `AuditEventType.Configure`. Phase-definition create/update write `AuditEventType.TestChange`. Activate/deactivate remain Activate/Deactivate. | URS-BB-075, URS-BB-011 | `ExceptionDefinitionAdminService`; `PhaseDefinitionAdminService` | `AdminCatalogTests` exception and phase audit tests |
| FRS-BB-110 | Creating or updating a blood-attribute definition requires `admin.config.edit` in `BloodAttributeAdminService`. Activating or deactivating requires `admin.config.activate`. Callers that skip the API cannot change antigen-negative selection. | URS-BB-076 | `BloodAttributeAdminService`; `BloodAttributeAuthorizationRule` | `BloodAttributeAuthorizationRuleTests`; `BloodAttributeAuthorizationTests` |
| FRS-BB-111 | Order/test rule-definition create/update write `AuditEventType.Configure`. Test-grouper create/update write `AuditEventType.TestChange`. Activate/deactivate remain Activate/Deactivate. | URS-BB-077, URS-BB-011 | `RuleDefinitionAdminService`; `TestGrouperAdminService` | `RulesEngineTests` rule-admin audit assertions; `SubtestAndGrouperAdminTests` |
| FRS-BB-112 | Creating or updating a reflex rule requires `admin.tests.manage` in `ReflexRuleAdminService`. Activating or deactivating requires `admin.config.activate`. Callers that skip the API cannot change auto-ordered follow-up tests. | URS-BB-078 | `ReflexRuleAdminService`; `ReflexRuleAuthorizationRule` | `ReflexRuleAuthorizationRuleTests`; `ReflexRuleAuthorizationTests` |
| FRS-BB-113 | Creating or updating a subtest definition requires `admin.tests.manage` in `SubtestDefinitionAdminService`. Activating or deactivating requires `admin.config.activate`. Callers that skip the API cannot change panel interpretation choices. | URS-BB-079 | `SubtestDefinitionAdminService`; `SubtestCatalogAuthorizationRule` | `SubtestCatalogAuthorizationRuleTests`; `SubtestDefinitionAuthorizationTests` |
| FRS-BB-114 | Specimen-type create/update write `AuditEventType.TestChange`. Activate/deactivate remain Activate/Deactivate. | URS-BB-080, URS-BB-011 | `SpecimenTypeAdminService` | `AdminCatalogTests` specimen-type audit tests |
| FRS-BB-115 | Creating or updating a test grouper requires `admin.tests.manage` in `TestGrouperAdminService`. Activating or deactivating requires `admin.config.activate`. Callers that skip the API cannot drop a member test from Type and Screen. | URS-BB-081 | `TestGrouperAdminService`; `TestGrouperAuthorizationRule` | `TestGrouperAuthorizationRuleTests`; `TestGrouperAuthorizationTests` |
| FRS-BB-116 | HL7 endpoint create/update write `AuditEventType.Interface`. Enable/disable remain Activate/Deactivate. | URS-BB-082, URS-BB-011, URS-BB-013 | `Hl7ConfigAdminService` | `AdminInterfaceSetupTests` |
| FRS-BB-117 | Creating or updating an order or test rule requires `admin.tests.manage` in `RuleDefinitionAdminService`. Activating or deactivating requires `admin.config.activate`. Callers that skip the API cannot add or cancel follow-up tests. | URS-BB-083 | `RuleDefinitionAdminService`; `RuleDefinitionAuthorizationRule` | `RuleDefinitionAuthorizationRuleTests`; `RuleDefinitionAuthorizationTests` |
| FRS-BB-118 | Creating or updating a phase definition requires `admin.tests.manage` in `PhaseDefinitionAdminService`. Activating or deactivating requires `admin.config.activate`. Callers that skip the API cannot change result-entry phases or interpretation inclusion. | URS-BB-084 | `PhaseDefinitionAdminService`; `PhaseCatalogAuthorizationRule` | `PhaseCatalogAuthorizationRuleTests`; `PhaseDefinitionAuthorizationTests` |
| FRS-BB-119 | Compatibility-table version and rule create/update write `AuditEventType.Configure`. Activate/deactivate remain Activate/Deactivate. | URS-BB-085, URS-BB-011 | `CompatibilityRuleAdminService` | `CompatibilityRuleAdminTests` |
| FRS-BB-120 | Creating or updating an exception definition requires `admin.config.edit` in `ExceptionDefinitionAdminService`. Activating or deactivating requires `admin.config.activate`. Callers that skip the API cannot change which HardStops are overridable at result verify. | URS-BB-086 | `ExceptionDefinitionAdminService`; `ExceptionCatalogAuthorizationRule` | `ExceptionCatalogAuthorizationRuleTests`; `ExceptionDefinitionAuthorizationTests` |
| FRS-BB-121 | Special-requirement add writes `AuditEventType.Antibody` after save so the event has an entity id, type, antigen, and reason. Deactivate writes `Deactivate` with old/new. | URS-BB-087, URS-BB-011 | `SpecialRequirementService` | `Phase3ServicesTests.SpecialRequirement_AddAndDeactivate_WriteNamedAudits` |
| FRS-BB-122 | Creating or updating a specimen-type definition requires `admin.config.edit` in `SpecimenTypeAdminService`. Activating or deactivating requires `admin.config.activate`. Callers that skip the API cannot exclude a test from a specimen type. | URS-BB-088 | `SpecimenTypeAdminService`; `SpecimenTypeAuthorizationRule` | `SpecimenTypeAuthorizationRuleTests`; `SpecimenTypeAuthorizationTests` |
| FRS-BB-123 | Creating or updating a product definition requires `admin.products.manage` in `ProductAdminService`. Activating or deactivating requires `admin.config.activate`. Callers that skip the API cannot change which products require crossmatch, ABO/Rh match, or retype. | URS-BB-089 | `ProductAdminService`; `ProductCatalogAuthorizationRule` | `ProductCatalogAuthorizationRuleTests`; `ProductCatalogAuthorizationTests` |
| FRS-BB-124 | Test worklist items include `CurrentResultSource`. The global worklist, patient Tests tab, and specimen-linked test table display it. | URS-BB-090, URS-BB-047 | `TestWorklistService`; `TestWorkItemDto` | `TestWorklistTests.PendingWorklist_IncludesInstrumentSource` |
| FRS-BB-125 | Creating or updating a modification rule requires `admin.modification-rules.manage` in `ModificationRuleAdminService`. Activating or deactivating requires `admin.config.activate`. Callers that skip the API cannot change which source product, modification type, target product, or expiration offset is allowed. | URS-BB-091 | `ModificationRuleAdminService`; `ModificationRuleAuthorizationRule` | `ModificationRuleAuthorizationRuleTests`; `ModificationRuleAuthorizationTests` |
| FRS-BB-126 | Product-definition create/update write `AuditEventType.Configure`. Activate/deactivate remain Activate/Deactivate. | URS-BB-092, URS-BB-011, URS-BB-018 | `ProductAdminService` | `ProductCatalogAuthorizationTests.CreateAndUpdate_WriteConfigure` |
| FRS-BB-127 | Creating or updating an expiration modification code requires `admin.modification-rules.manage` in `ExpirationModificationCodeAdminService`. Activating or deactivating requires `admin.config.activate`. Callers that skip the API cannot change how long a modified unit remains usable. | URS-BB-093 | `ExpirationModificationCodeAdminService`; `ExpirationModificationCodeAuthorizationRule` | `ExpirationModificationCodeAuthorizationRuleTests`; `ExpirationModificationCodeAuthorizationTests` |
| FRS-BB-128 | Creating, updating, enabling, or disabling an HL7 endpoint requires `admin.hl7.manage` in `Hl7ConfigAdminService`. Callers that skip the API cannot add or enable an inbound results (ORU) endpoint. | URS-BB-094 | `Hl7ConfigAdminService`; `Hl7EndpointAuthorizationRule` | `Hl7EndpointAuthorizationRuleTests`; `Hl7EndpointAuthorizationTests` |
| FRS-BB-129 | Expiration-modification-code create/update write `AuditEventType.Configure`. Activate/deactivate remain Activate/Deactivate. | URS-BB-095, URS-BB-011, URS-BB-093 | `ExpirationModificationCodeAdminService` | `ExpirationModificationCodeAuthorizationTests.CreateAndUpdate_WriteConfigure` |
| FRS-BB-130 | Replacing HL7 value translations requires `admin.hl7.manage` in `InterfaceTranslationAdminService`. Callers that skip the API cannot change how inbound result values map to internal codes. | URS-BB-096 | `InterfaceTranslationAdminService`; `InterfaceTranslationAuthorizationRule` | `InterfaceTranslationAuthorizationRuleTests`; `InterfaceTranslationAuthorizationTests` |
| FRS-BB-131 | Modification-rule create/update write `AuditEventType.Configure`. Activate/deactivate remain Activate/Deactivate. | URS-BB-097, URS-BB-011, URS-BB-091 | `ModificationRuleAdminService` | `ModificationRuleAuthorizationTests.CreateAndUpdate_WriteConfigure` |
| FRS-BB-132 | Creating or updating a charge code requires `admin.config.edit` in `ChargeCodeAdminService`. Activating or deactivating requires `admin.config.activate`. Callers that skip the API cannot change which codes are billed after issue. | URS-BB-098 | `ChargeCodeAdminService`; `ChargeCodeAuthorizationRule` | `ChargeCodeAuthorizationRuleTests`; `ChargeCodeAuthorizationTests` |
| FRS-BB-133 | Creating or updating a charge rule requires `admin.config.edit` in `ChargeRuleAdminService`. Activating or deactivating requires `admin.config.activate`. Callers that skip the API cannot change which trigger bills after issue. | URS-BB-099 | `ChargeRuleAdminService`; `ChargeRuleAuthorizationRule` | `ChargeRuleAuthorizationRuleTests`; `ChargeRuleAuthorizationTests` |
| FRS-BB-134 | Ward receipt of an issued unit writes `AuditEventType.Transfusion` on the issue (who/when/late), not generic Update. | URS-BB-100, URS-BB-011, URS-BB-040 | `IssuingService.RecordWardReceiptAsync` | `IssuingReturnAuthorizationTests.WardReceipt_WithoutTransfusionDocument_IsHardStopped` |
| FRS-BB-135 | Creating or updating a product billing row requires `admin.config.edit` in `ProductBillingAdminService`. Activating or deactivating requires `admin.config.activate`. Callers that skip the API cannot change which ISBT product bills after issue. | URS-BB-101 | `ProductBillingAdminService`; `ProductBillingAuthorizationRule` | `ProductBillingAuthorizationRuleTests`; `ProductBillingAuthorizationTests` |
| FRS-BB-136 | Directed-to-allogeneic conversion writes `AuditEventType.ProductStatus` on the unit (old/new restriction, reserved patient, reason, second verifier), not generic Update. | URS-BB-102, URS-BB-011, URS-BB-024, URS-BB-010 | `InventoryService.ConvertDirectedToAllogeneicAsync` | `InventoryServiceTests.ConvertDirected_FromQuarantine_ClearsReservation` |
| FRS-BB-137 | Creating or updating a test/service billing row requires `admin.config.edit` in `TestServiceBillingAdminService`. Activating or deactivating requires `admin.config.activate`. Callers that skip the API cannot change which verified test bills after release. | URS-BB-103 | `TestServiceBillingAdminService`; `TestServiceBillingAuthorizationRule` | `TestServiceBillingAuthorizationRuleTests`; `TestServiceBillingAuthorizationTests` |
| FRS-BB-138 | Creating or updating an ordering provider requires `admin.config.edit` in `OrderingProviderAdminService`. Activating or deactivating requires `admin.config.activate`. Callers that skip the API cannot change who can be named on an order. | URS-BB-104 | `OrderingProviderAdminService`; `OrderingProviderAuthorizationRule` | `OrderingProviderAuthorizationRuleTests`; `OrderingProviderAuthorizationTests` |
| FRS-BB-139 | Directory user create/update and role create write `AuditEventType.UserRole`. Lock/password-reset remain Configure. Activate/deactivate remain Activate/Deactivate. | URS-BB-105, URS-BB-011, URS-BB-068 | `UserAdminService` | `UserAdminAuthorizationTests.CreateAndUpdate_WriteUserRole` |
| FRS-BB-140 | Creating or updating an ordering location requires `admin.config.edit` in `OrderingLocationAdminService`. Activating or deactivating requires `admin.config.activate`. Callers that skip the API cannot change which ward can be named on an order. | URS-BB-106 | `OrderingLocationAdminService`; `OrderingLocationAuthorizationRule` | `OrderingLocationAuthorizationRuleTests`; `OrderingLocationAuthorizationTests` |
| FRS-BB-141 | Antibody panel-lot create writes `AuditEventType.TestChange`. Activate/deactivate remain Activate/Deactivate. Create does not identify or post antibodies. | URS-BB-107, URS-BB-011 | `AntibodyPanelLotAdminService` | `AntibodyPanelLotAdminTests.Create_WithCellAndAntigen_WritesAudit_AndDoesNotIdentify` |
| FRS-BB-142 | Reviewing a captured charge requires `billing.review` in `BillingService`. Cancelling requires `billing.cancel`. Exporting requires `billing.export`. Capture and the review queue stay ungated so clinical paths can still write pending events. | URS-BB-108 | `BillingService`; `BillingAuthorizationRule` | `BillingAuthorizationRuleTests`; `BillingAuthorizationTests` |
| FRS-BB-143 | Creating or updating an inventory location requires `admin.config.edit` in `InventoryLocationAdminService`. Activating or deactivating requires `admin.config.activate`. Callers that skip the API cannot change where units can be stored or issued. | URS-BB-109 | `InventoryLocationAdminService`; `InventoryLocationAuthorizationRule` | `InventoryLocationAuthorizationRuleTests`; `InventoryLocationAuthorizationTests` |
| FRS-BB-144 | Inventory-location create/update write `AuditEventType.Configure`. Activate/deactivate remain Activate/Deactivate. | URS-BB-110, URS-BB-011, URS-BB-109, URS-BB-010 | `InventoryLocationAdminService` | `InventoryLocationAuthorizationTests.CreateAndUpdate_WriteConfigure` |
| FRS-BB-145 | Updating a facility policy requires `admin.config.edit` in `FacilityPolicyAdminService`. Callers that skip the API cannot change electronic XM, second ABO, uncrossmatched O, specimen windows, or other facility switches. List still materializes missing catalog defaults. | URS-BB-111 | `FacilityPolicyAdminService`; `FacilityPolicyAuthorizationRule` | `FacilityPolicyAuthorizationRuleTests`; `FacilityPolicyAuthorizationTests` |
| FRS-BB-146 | Charge-code create/update write `AuditEventType.Configure`. Activate/deactivate remain Activate/Deactivate. | URS-BB-112, URS-BB-011, URS-BB-098 | `ChargeCodeAdminService` | `ChargeCodeAuthorizationTests.CreateAndUpdate_WriteConfigure` |
| FRS-BB-147 | DIN lookback search and recipient traceback require `lookback.manage` in `LookbackService`. Callers that skip the API cannot list who received a donation. Recall and notification attempt stay separately gated. | URS-BB-113 | `LookbackService`; `LookbackAuthorizationRule` | `LookbackAuthorizationRuleTests`; `LookbackSearchAuthorizationTests` |
| FRS-BB-148 | Charge-rule create/update write `AuditEventType.Configure`. Activate/deactivate remain Activate/Deactivate. | URS-BB-114, URS-BB-011, URS-BB-099 | `ChargeRuleAdminService` | `ChargeRuleAuthorizationTests.CreateAndUpdate_WriteConfigure` |
| FRS-BB-149 | Product-billing create/update write `AuditEventType.Configure`. Activate/deactivate remain Activate/Deactivate. | URS-BB-115, URS-BB-011, URS-BB-101 | `ProductBillingAdminService` | `ProductBillingAuthorizationTests.CreateAndUpdate_WriteConfigure` |
| FRS-BB-150 | Test/service-billing create/update write `AuditEventType.Configure`. Activate/deactivate remain Activate/Deactivate. | URS-BB-116, URS-BB-011, URS-BB-103 | `TestServiceBillingAdminService` | `TestServiceBillingAuthorizationTests.CreateAndUpdate_WriteConfigure` |
| FRS-BB-151 | Ordering-provider create/update write `AuditEventType.Configure`. Activate/deactivate remain Activate/Deactivate. | URS-BB-117, URS-BB-011, URS-BB-104 | `OrderingProviderAdminService` | `OrderingProviderAuthorizationTests.CreateAndUpdate_WriteConfigure` |
| FRS-BB-152 | Ordering-location create/update write `AuditEventType.Configure`. Activate/deactivate remain Activate/Deactivate. | URS-BB-118, URS-BB-011, URS-BB-106 | `OrderingLocationAdminService` | `OrderingLocationAuthorizationTests.CreateAndUpdate_WriteConfigure` |
| FRS-BB-153 | Patient demographic update writes `AuditEventType.PatientAccess` with old/new name, date of birth, sex, status, and pregnancy. Chart-open remains PatientAccess with reason "Patient record opened." Create stays interceptor Create. | URS-BB-119, URS-BB-011, URS-BB-031 | `PatientService.UpdateAsync` | `PatientServiceTests.Update_WritesPatientAccess` |
| FRS-BB-154 | Charge review and cancel write `AuditEventType.Billing` with old/new status (cancel includes the reason). Export writes `AuditEventType.Export`. Capture stays interceptor Create. | URS-BB-120, URS-BB-011, URS-BB-108 | `BillingService` | `BillingAuthorizationTests.ReviewAndCancel_WriteBilling`; `BillingAuthorizationTests.Export_WritesExport`; `Phase7BillingTests.Cancel_RequiresReason_AndWritesAuditEvent` |
| FRS-BB-155 | HL7 value-translation replace writes `AuditEventType.Interface` with old/new rows for that data item. Endpoint create/update remain Interface. | URS-BB-121, URS-BB-011, URS-BB-096, URS-BB-013 | `InterfaceTranslationAdminService` | `InterfaceTranslationAuthorizationTests.Replace_WritesInterface` |
| FRS-BB-156 | Patient create writes `AuditEventType.PatientAccess` with MRN, name, date of birth, and sex. Chart-open and demographic update remain PatientAccess with distinct reasons. Interceptor Create still writes. ADT inbound still bypasses `PatientService`. | URS-BB-122, URS-BB-011, URS-BB-045 | `PatientService.CreateAsync` | `PatientServiceTests.Create_WritesPatientAccess` |
| FRS-BB-157 | Workspace visit create/update write `AuditEventType.PatientAccess` on `Encounter` with visit/location/status old/new. ADT upsert and HL7 order-encounter ensure stay ungated and interceptor-only. | URS-BB-123, URS-BB-011, URS-BB-058 | `EncounterService` | `EncounterServiceAuthorizationTests.CreateAndUpdate_WritePatientAccess` |
| FRS-BB-158 | Worklist and specimen panel entry forms state that catalog-matching panel entry is stored as `ResultSource.Calculated`, and save feedback names the stored source. Instrument and Interface keep their source. This is UX for `ResultProvenanceRule`, not a new regulatory claim. | URS-BB-124, URS-BB-047, URS-BB-056, URS-BB-090 | `ResultSourceEntryCopy`; `TestResultEntryPanel`; `PatientDetail` | `ResultSourceEntryCopyTests` |
| FRS-BB-159 | The specimen typed-entry form (test code + value, no panel) states that the stored source is Manual, and enter feedback names the stored source. Instrument and Interface keep their source. This is UX for `ResultProvenanceRule`, not a new regulatory claim. | URS-BB-125, URS-BB-047, URS-BB-090 | `ResultSourceEntryCopy`; `PatientDetail` | `ResultSourceEntryCopyTests` |
| FRS-BB-160 | First specimen, compatibility, and component label prints write `AuditEventType.Print` after the job has an id. Reprint remains Reprint. Interceptor Create still writes. | URS-BB-126, URS-BB-011, URS-BB-055 | `PrintService.CreateJobAsync` | `Phase6PrintingTests.PrintSpecimenLabel_WritesPrint` |
| FRS-BB-161 | Antibody-identification workup open, specimen link, lot attach, assist refresh, interpretation, void, and stale-panel write `AuditEventType.Antibody` on the workup. Supervisor accept and complete remain Verify. Posted Identified findings remain Antibody on `AntibodyHistory`. | URS-BB-127, URS-BB-011, URS-BB-063 | `AntibodyIdentificationService` | `AntibodyIdentificationWorkupTests.CreateWorkup_WritesAntibody` |
| FRS-BB-162 | Deviation create writes `AuditEventType.Deviation` after the row has an id. Status update writes Deviation with old/new status. Interceptor Create/Update still write. List stays ungated. | URS-BB-128, URS-BB-011, URS-BB-044 | `DeviationService` | `DeviationServiceTests.CreateAndUpdateStatus_WriteDeviation` |
| FRS-BB-163 | Reaction-investigation open writes `AuditEventType.ReactionInvestigation` after the row has an id. Workup update, CBER notification, and written-report timestamps write ReactionInvestigation with old/new. Opening from a suspected transfusion stays automatic and ungated. Interceptor Create/Update still write. | URS-BB-129, URS-BB-011, URS-BB-036 | `ReactionInvestigationService` | `ReactionInvestigationServiceTests.OpenAndUpdate_WriteReactionInvestigation` |
| FRS-BB-164 | Specimen-card verify, ABO/Rh-override verify, correct, submit-for-verification, and invalidate feedback name the stored `ResultSource`. Instrument and Interface keep their source. This is UX for `ResultProvenanceRule`, not a new regulatory claim. | URS-BB-130, URS-BB-047, URS-BB-090 | `ResultSourceEntryCopy`; `PatientDetail` | `ResultSourceEntryCopyTests` |
| FRS-BB-165 | DIN lookback recall writes `AuditEventType.Lookback` on a collected unit id with DIN/unit/notification counts. Notification-attempt recording writes Lookback on `LookbackNotification` with status/physician/notes old/new. Search and traceback remain Lookback. Interceptor Create/Update still write. The LIS does not auto-notify patients. | URS-BB-131, URS-BB-011, URS-BB-022, URS-BB-053 | `LookbackService` | `LookbackSearchAuthorizationTests.RecallAndRecordAttempt_WriteLookback` |
| FRS-BB-166 | Submit-for-verification and invalidate write stored `ResultSource` on old/new (`Result` / `Invalidate`). New-version invalidate, restore-prior-verified invalidate, and in-place invalidate all include source. Verify and correct already stored source. Interceptor Create/Update still write. | URS-BB-132, URS-BB-011, URS-BB-047 | `ResultService` | `ResultLifecycleTests.SubmitThenVerify_WritesVerifyAudit_WithOldAndNew`; `ResultLifecycleTests.InvalidateVerified_CreatesNewVersion_AndRetainsOriginal`; `ResultLifecycleTests.InvalidateUnverifiedCorrection_RestoresPriorVerified` |
| FRS-BB-167 | Verify writes `ResultSource` on old and new. Re-entry after invalidation writes prior and replacement source on old/new (`Result`, reason "Re-entry after invalidation."). Interface replacement after invalidate uses this path. Interceptor Create/Update still write. | URS-BB-133, URS-BB-011, URS-BB-047 | `ResultService` | `ResultLifecycleTests.SubmitThenVerify_WritesVerifyAudit_WithOldAndNew`; `ResultLifecycleTests.ReenterAfterInvalidate_WritesSourceOnOldAndNew` |
| FRS-BB-168 | Unit ABO/Rh retype record writes `AuditEventType.Result` with interpreted ABO/Rh and immediately writes interpreted ABO/Rh on `Verify` old/new. ProductStatus on release/quarantine stays as-is. Interceptor Create/Update still write. | URS-BB-134, URS-BB-011, URS-BB-047 | `ProductRetypeService` | `ProductRetypeServiceTests.Record_WriteInterpretedType` |
| FRS-BB-169 | Unit retype record feedback names the interpreted ABO/Rh. Available vs Quarantine wording stays. This is UX for the stored interpretation, not a new regulatory claim. | URS-BB-135, URS-BB-047 | `ProductRetypeEntryCopy`; `ProductRetypeEntryPanel` | `ProductRetypeEntryCopyTests` |
| FRS-BB-170 | Compatibility-page crossmatch save feedback names method and Compatible or Incompatible plus the row id. Named Crossmatch audit stays as-is. This is UX for the stored XM result, not a new regulatory claim. | URS-BB-136, URS-BB-047 | `CrossmatchEntryCopy`; `Compatibility` | `CrossmatchEntryCopyTests` |
| FRS-BB-171 | Worklist expand-to-enter XM save feedback names stored `ResultSource` plus method and Compatible or Incompatible. Non-XM save feedback stays source-only. This is UX for the stored XM result, not a new regulatory claim. | URS-BB-137, URS-BB-047, URS-BB-090 | `CrossmatchEntryCopy`; `TestResultEntryPanel` | `CrossmatchEntryCopyTests` |
| FRS-BB-172 | Unit ISBT identity correction writes `AuditEventType.Correct` on the unit after the correction row has an id, with field, old/new value, and `CorrectionId`. Privilege check stays `inventory.correct-identity`. Interceptor Create/Update still write. | URS-BB-138, URS-BB-011, URS-BB-033 | `ComponentIdentityCorrectionService` | `ComponentIdentityCorrectionServiceTests.Correct_WritesCorrectWithCorrectionId` |
| FRS-BB-173 | Verified ABID (free-text/coded, `ContributesToAntibodyHistory`) posts Identified history then writes `AuditEventType.Antibody` on `AntibodyHistory` with the history row id, `PatientId`, `SourceResultId`, specificities, and `HistoryIds`. Workup-complete posting still uses patient id as entity id (OCD-023). Interceptor Create/Update still write. | URS-BB-139, URS-BB-011, URS-BB-047 | `ResultService.ApplyAntibodyIdentificationResultAsync` | `AntibodyIdentificationTests.VerifyAbid_WritesAntibodyWithHistoryIds` |
| FRS-BB-174 | Completing a reviewed antibody-identification workup posts Identified history then writes `AuditEventType.Antibody` on `AntibodyHistory` with the history row id, `PatientId`, `WorkupId`, and `HistoryId`. Dedup when the specificity is already active stays silent. Dual posting paths remain (OCD-023). Interceptor Create/Update still write. | URS-BB-140, URS-BB-011, URS-BB-063 | `AntibodyIdentificationService.PostHistoryAsync` | `AntibodyIdentificationWorkupTests.Complete_WritesAntibodyWithHistoryId` |
| FRS-BB-175 | A matching test-level order/test rule at result verify writes `AuditEventType.OrderChange` on the order with rule code, level, `TestResultId`, actions, and notes. Rule-definition create/update remain Configure. Catalog reflex-rule adds remain OrderChange on `OrderLine`. Interceptor Create/Update still write. | URS-BB-141, URS-BB-011, URS-BB-077 | `RuleEngineService.ApplyTestRulesAsync` | `RulesEngineTests.WeakDRule_WritesOrderChangeOnMatch` |
| FRS-BB-176 | A matching order-level rule at create/update writes `AuditEventType.OrderChange` on the order after it has an id, with rule code, level, actions, and notes. Blocked orders are not persisted and write no match event. Order-created OrderChange stays. Interceptor Create/Update still write. | URS-BB-142, URS-BB-011, URS-BB-077 | `RuleEngineService.PersistOrderLogsAsync` | `RulesEngineTests.NeonatalRule_WritesOrderChangeOnMatch` |
| FRS-BB-177 | A catalog reflex-rule add at result verify writes `AuditEventType.OrderChange` on `OrderLine` after the line has an id, with `OrderId`, `LineId`, trigger result, and reflex test/rule codes. Configurable test-rule matches remain OrderChange on the order. Interceptor Create/Update still write. | URS-BB-143, URS-BB-011, URS-BB-061 | `ResultService.ApplyReflexRulesAsync` | `ReflexRulesTests.VerifyPositiveAbsc_AddsAbidLine_AndShowsOnWorklist` |
| FRS-BB-178 | Allocating or releasing a reserved unit writes `AuditEventType.Assignment` on `Allocation` with unit number, DIN, and blood-product id on old/new. Patient-workspace reserve uses the same Compatibility allocate path. Privilege, XM gates, and interceptor Create/Update stay. | URS-BB-144, URS-BB-011, URS-BB-010 | `CompatibilityService` | `Phase4IssuingTests.AllocateAndRelease_WriteAssignmentWithUnitIdentity` |
| FRS-BB-179 | Recording ward receipt writes `AuditEventType.Transfusion` on `Issue` with unit number, DIN, blood-product id, and `IssueId` on old/new. Entity id stays the issue id. Privilege, appearance, and scan gates stay. HL7-BPAM still stamps ward receipt on the document-transfusion event only. Interceptor Create/Update still write. | URS-BB-145, URS-BB-011, URS-BB-040 | `IssuingService.RecordWardReceiptAsync` | `Phase4IssuingTests.WardReceipt_WritesTransfusionWithUnitIdentity` |
| FRS-BB-180 | Issuing a unit, or recording a blocked issue attempt, writes `AuditEventType.Issue` or `EmergencyRelease` on `BloodUnit` with unit number, DIN, and blood-product id on old/new. Entity id stays the unit id. Override events, privilege, and issue gates stay. Interceptor Create/Update still write. | URS-BB-146, URS-BB-011, URS-BB-006 | `IssuingService.IssueUnitAsync` | `Phase4IssuingTests.Issue_WritesIssueWithUnitIdentity` |
| FRS-BB-181 | Interactive API identity comes from a server-issued session (`Authorization: Bearer`), not a client-supplied `X-User` header. Login fail-closes when `PasswordHash` is missing (except Development DevMode). The raw token is returned once and stored as a hash. Idle (30 min) and absolute (12 h) expiry, logout, lock, deactivate, and five failed sign-ins revoke or reject the session. `Auth:AllowLegacyIdentityHeader` defaults off and is forced off in Production. Application permission HardStops are unchanged. | URS-BB-147, URS-BB-011 | `AuthSession`; `AuthSessionService`; `RequestIdentityResolver`; `AuthSessionMiddleware`; `HttpCurrentUser` | `IdentitySpoofingRegressionTests`; `AuthSessionServiceTests`; `AuthSessionValidityRuleTests` |
| FRS-BB-182 | Assigning roles, updating a user with a role list, or updating a role's permissions revokes outstanding sessions for the affected user(s). API `RequirePermission` and Application HardStops already re-read the directory on each request. The Web circuit refreshes `/api/me` and signs out on 401/404. | URS-BB-148, URS-BB-147 | `UserAdminService`; `UserSession.TryApplyLiveProfile` | `AuthSessionServiceTests.AssignRoles_RevokesOutstandingSessions`; `AuthSessionServiceTests.UpdateRolePermissions_RevokesSessionsForRoleMembers` |
| FRS-BB-183 | Recording an electronic XM re-reads antibody history (`HasAntibodyHistoryAsync`, including deactivated rows), screen, ABO, and open workups immediately before save, not only when the eligibility board was assessed. History posted after a green assess HardStops `XM-EC-HISTORY`. Clinical defaults (OCD-001) are unchanged. | URS-BB-149, URS-BB-009 | `CompatibilityService.EvaluateElectronicEligibilityAsync` | `ElectronicXmHistoryRegressionTests.AntibodyAddedAfterEligibleAssess_BlocksElectronicXmRecord`; `Phase4IssuingTests.ElectronicCrossmatch_WithAntibodyHistory_IsBlocked` |
| FRS-BB-184 | Each new `AuditEvent` stores `PreviousHash` and `RecordHash` (SHA-256 of the canonical payload plus the previous hash). The hashed list is a linked list from `GENESIS`; insert/id order is not required. Unique `PreviousHash` rejects a fork. Application saves still reject audit update/delete. Rows written before the chain are not rewritten and are not covered. Clinical and audit rows are not purged (OCD-007); retention years remain metadata only. | URS-BB-150, URS-BB-011 | `AuditHashChainRule`; `BloodBankDbContext.StampAuditHashChain` | TEST-BB-007; TEST-BB-008 |
| FRS-BB-185 | `DocumentTransfusionAsync` evaluates `PatientIdentityMatchRule` against two independent tokens that must match the issued patient. Electronic dual-ID (`TX-DUAL-ID`) is complete only when that match passes and an ISBT bedside unit scan verifies. A boolean PPID flag is not accepted. Legacy units without `ComponentIdentity` do not invent a new scan requirement. `RequireSecondVerifier` and directory second-user checks are unchanged (OCD-008). Interface BPAM documentation is unchanged. | URS-BB-151 | `IssuingService.DocumentTransfusionAsync`; `PatientIdentityMatchRule`; `DualIdentificationRule` | `Phase4IssuingTests.Transfusion_MissingPatientIdentifiers_IsHardStopped`; `Phase4IssuingTests.Transfusion_MismatchedPatientIdentifiers_IsHardStopped`; `Phase4IssuingTests.Transfusion_IsbtUnit_RequiresIdentityAndMatchingScan`; `Phase4IssuingTests.Transfusion_RequireSecondVerifier_WithoutElectronicId_NeedsDirectoryUser` |
| FRS-BB-186 | Licensed ISBT catalog import requires `admin.config.edit`, an ICCBBA license acknowledgment, and a `StandardVersion` that is not a placeholder or pending-ICCBBA token. Rows must be supplied by the caller. Placeholder rows with the same product or ABO code are replaced and marked not-placeholder. Import writes `AuditEventType.Import`. No ICCBBA product or ABO/RhD codes are invented or shipped. | URS-BB-152 | `IsbtLicensedCatalogImportRule`; `IsbtProductCodeAdminService.ImportLicensedAsync` | `IsbtLicensedCatalogImportRuleTests`; `IsbtLicensedCatalogImportTests` |
| FRS-BB-187 | HTTP `POST /api/hl7/inbound` requires an authenticated session and `hl7.manage`. `MllpListenerService` and `Hl7FileDropService` accept framed or dropped messages without an application credential (OCD-034). No shared interface secret is invented or stored. | URS-BB-153 | `Hl7Endpoints`; `MllpListenerService`; `Hl7FileDropService` | `Hl7EndpointAuthorizationTests`; `docs/hl7-design.md` transport authentication |
| FRS-BB-188 | API and Web apply `HttpSecurityHeaderPolicy` headers (nosniff, DENY framing, CSP, Permissions-Policy). API also sends `Cache-Control: no-store`. `Cors:AllowAnyOrigin` HardStops outside Development (`SEC-CORS-ANY`). Explicit `Cors:AllowedOrigins` are optional. Interactive identity remains Bearer in circuit memory; Blazor Server keeps `UseAntiforgery`. Web CSP still allows inline script/style and eval for the circuit. | URS-BB-154 | `HttpSecurityHeaderPolicy`; `SecurityHeadersMiddleware` | TEST-BB-016 (`HttpSecurityHeaderPolicyTests`) |
| FRS-BB-189 | Formal `TEST-BB-*` IDs for Cycles 1–9 live in `docs/validation/TEST_CATALOG.md` and must stay unique. Living architecture, safety-rules, workflows, ERD, and validation-plan headers are not Phase 0 drafts. Clinical and audit rows are not purged (OCD-007). This is not a certification claim. | URS-BB-155 | `docs/validation/TEST_CATALOG.md` | TEST-BB-017 (`TestCatalogPackagingTests`) |
| FRS-BB-190 | `GET /api/compliance/downtime-reconciliation` (`audit.read`) returns a read-only count of unresolved interface errors, pending outbound HL7, open issues, pending retrospective crossmatches, hashed/unhashed audit rows, and `AuditHashChainRule.Verify`. `/downtime` displays that snapshot. Paper OCR, multi-site failover, and a purge job are not implemented. Neonatal irradiation/CMV product defaults stay as order-rule hooks (OCD-035). | URS-BB-156, URS-BB-014 | `DowntimeReconciliationService`; `DowntimeReconciliationAuthorizationRule` | TEST-BB-018; TEST-BB-019 |
| FRS-BB-191 | `docs/validation/TEST_CATALOG.md` assigns TEST-BB-020–025 for issue-gate, merged-patient, open-workup electronic XM, and lookback evidence. `TestCatalogPackagingTests` asserts unique IDs and that each cited class exists under `tests/`. Remaining tests may stay class-name cited. This is not a certification claim. | URS-BB-157 | `docs/validation/TEST_CATALOG.md` | TEST-BB-017; TEST-BB-025 |
| FRS-BB-192 | `docs/validation/TEST_CATALOG.md` assigns TEST-BB-026–030 for allocation/issue uniqueness, patient merge, unit retype, emergency-issue privilege, and immuno privilege. Catalog packaging tests still apply. Remaining tests may stay class-name cited. This is not a certification claim. | URS-BB-158 | `docs/validation/TEST_CATALOG.md` | TEST-BB-025; TEST-BB-026; TEST-BB-027; TEST-BB-028; TEST-BB-029; TEST-BB-030 |
| FRS-BB-193 | `IccbbaExtractParser` accepts facility-exported CSV, TSV, TXT, or JSON. Access/Excel are rejected. `GET /api/admin/isbt-product-codes/extracts` lists files in `Iccbba:ExtractDirectory` (default `testdata/isbt128/extracts`) or an empty list. `POST .../import-extract` and `POST .../import-extract-upload` parse then call `ImportLicensedAsync` (same license/version/empty gates). An empty folder is a no-op. Startup does not auto-import. Seed does not revert licensed non-placeholder product rows. No ICCBBA codes are invented or shipped. | URS-BB-159, URS-BB-152 | `IccbbaExtractParser`; `IccbbaExtractDirectory`; `IsbtProductCodeAdminService.ImportExtractFilesAsync` | TEST-BB-031; TEST-BB-032; TEST-BB-033 |
| FRS-BB-194 | Admin Crossmatch Settings store default XM/CXM tests and electronic XM visit/specimen/test minimums. After a current antibody screen is verified, the configured serologic test attaches to open product orders unless the patient is EXM-eligible. Changing complex to simple XM requires `ALLOC-XM-AB-HISTORY` and writes an append-only `Override`. Eligible patients with a current type and screen receive an auto-resulted EXM on the product order when a unit is selected. | URS-BB-160 | `CrossmatchSettings`; `CrossmatchAttachmentService`; `ElectronicCrossmatchEligibilityRule` | `CrossmatchSettingsValidatorTests`; `CrossmatchSettingsTests`; `CrossmatchRuleTests` |
| FRS-BB-195 | `TestWorkItemDto` includes current ABO/Rh, antibody-history summary, specimen expiration, and an expired flag. The global and patient test worklists display those fields. `/compatibility` selects the patient by MRN/name and the specimen by accession. Result-entry HardStops on expired specimens are unchanged. | URS-BB-161 | `TestWorklistService`; `TestWorklist.razor`; `Compatibility.razor` | TEST-BB-047 |
| FRS-BB-196 | `ExpectedInboundWorkItemDto` includes `ProductTypeId`. `/inventory` Expected inbound shows product and an inline Confirm that calls `ReceiveExpectedUnitAsync` with appearance, temperature, and second verifier. Drawer confirm remains. Receive gates are unchanged. | URS-BB-162 | `ExpectedInboundWorkItemDto`; `Inventory.razor` | TEST-BB-048 |
| FRS-BB-197 | `InTransitWorkItemDto` and `RetrospectiveCrossmatchWorkItemDto` include unit number, patient display name, current ABO/Rh, antibody-history summary, and specimen expiration. `/issuing` shows those fields, prefills ward receipt from an in-transit Receive action, and selects the issue patient by MRN/name. Issue-gate and identity-token rules are unchanged. | URS-BB-163 | `IssuingService`; `Issuing.razor` | TEST-BB-049 |
| FRS-BB-198 | `ReactionInvestigationDto` includes MRN, patient display name, unit number, current ABO/Rh, antibody-history summary, and workup-incomplete from `ReactionWorkupCompletenessRule`. `/reactions` lists those fields and remainder-hold status. `?id=` / `?patientId=` open the matching case. `/issuing` links to the opened workup after a suspected transfusion. Close and quarantine gates are unchanged. | URS-BB-164 | `ReactionInvestigationService`; `Reactions.razor` | TEST-BB-050 |
| FRS-BB-199 | `Hl7ErrorDto` includes message control id, type, trigger, and ACK from the linked log. `GET /api/hl7/errors` returns unresolved rows with that context. `/hl7` Replay and View use the existing replay endpoint. `ReplayAsync` marks the original error resolved when the replay is accepted. MSH-10 idempotency and AE mapping NAKs are unchanged. | URS-BB-165 | `Hl7InboundProcessor`; `Hl7.razor` | TEST-BB-051 |
| FRS-BB-200 | Demo seed `MRN0009` loads ADT/ORM/ORU artifacts with an accepted specimen and a `PendingVerification` interface ABSC. The pending worklist shows that source/value. `/test-worklist` Verify and `TestResultEntryPanel` call existing `VerifyResultAsync` without a second enter. ORU specimen and OBX-11 gates are unchanged. | URS-BB-166 | `DatabaseSeeder.SeedHl7DataLoadScenarioAsync`; `TestResultEntryPanel.razor` | TEST-BB-052 |
| FRS-BB-070 | Configuration catalogs are versioned where modeled and write `ConfigurationChangeHistory`. Runtime uses the single live row (`IsActive && !IsDraft`). As-of dating, scheduled activation, and overlapping versions are not implemented (OCD-033). | URS-BB-012 | Admin services | Admin catalog tests |
| FRS-BB-080 | HL7 inbound stores raw message, parse status, ACK, and error-queue entries. ADT/ORM follow a merged MRN to the survivor and do not write demographics onto the loser. ADT A18/A40 apply <c>PatientMergeService</c> when configured. | URS-BB-013, URS-BB-015 | `Hl7InboundProcessor`; `PatientMergeFollow` | Phase 5 HL7 merge tests |
| FRS-BB-090 | Downtime recovery is documented; duplicate prevention uses unique keys listed in `docs/DOWNTIME_PLAN.md`. The post-recovery snapshot is FRS-BB-190. | URS-BB-014 | Downtime plan; `DowntimeReconciliationService` | Operational checklist; TEST-BB-018; TEST-BB-019 |
