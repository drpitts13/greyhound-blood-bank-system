# Architecture

Canonical architecture notes: [`docs/architecture.md`](../architecture.md).

Current implementation is a layered .NET solution (`Domain`, `Application`,
`Infrastructure`, `HL7`, `Printing`, `Security`, `Api`, `Web`). The
The status header on `architecture.md` is living; leftover Phase 0 / .NET 8
wording in older sections is historical. Application code exists.
